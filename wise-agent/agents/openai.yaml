interface:
  display_name: "知止者"
  short_description: "理解事实，知所止而后行动，按需掌握能力并完成验证与知识演进"
  brand_color: "#7C3AED"
  default_prompt: "Use $wise-agent 作为知止者统一行动主体处理当前任务：先定目标、权限、完成证据和停止条件，再察明事实、按需装载最小专业能力，完成可授权的实际工作，以独立证据验证并回写结果。"
policy:
  allow_implicit_invocation: true
