# 产品方案与 PRD

本文用于生成或审查产品方案、PRD、评审材料和验收标准。它偏交付结构，不替代用户研究、市场调研或法务合规结论。需要可直接复用的完整文档骨架时，读取 `product-prd-template.md`；正式评审或提交前自检读取 `product-prd-quality-gates.md`；支付资金 PRD 协同 `payment-expert`；运营、通知、数据和发布附录读取 `product-prd-operations-and-data.md`。

## 使用时机

- 用户要求输出、完善、改写或评审产品方案、PRD、需求说明书、评审材料或验收标准。
- 用户要求规划一个需求跨哪些应用、客户端和页面落地，或要求页面清单、产品级页面标注、多端差异和跨应用衔接。
- 已通过 `product-scenario-routing.md` 判断目标产物是可评审产品文档。
- 复杂 PRD、AI 生成方案、原型候选或多方争议需要 PM / Reviewer、多视角、产品大师或 MAGI 式合议评审时，先读 `product-deliberation-workflow.md`，再回到本文生成或修订正文。
- 已通过资料、访谈、竞品、行业/政策或标杆实践形成产品洞察，需要先确认机会雷达、证据链和建议去向，再决定是否进入 PRD。
- 已通过问题地图或洞察整理形成机会清单，需要转成 Backlog、User Story、AC、优先级和可开发范围。
- 需要把对象、流程、状态、规则、运营、数据和风险写成可开发、可测试、可运营的结构。

## 不适用场景

- 用户要求完整可复制模板时，应读取 `product-prd-template.md`，不要只用本文。
- 用户要求 PRD 评审、提交前检查或符合性审查时，应读取 `product-prd-quality-gates.md`。
- PRD 涉及支付资金、账户账本、清结算、对账、VCC 或跨境时，应协同 `payment-expert`。
- PRD 涉及运营后台、人工处理、通知、埋点、报表或发布运营时，应读取 `product-prd-operations-and-data.md`。
- 涉及支付、资金、账户、账本、清结算、对账或跨境时，使用 `payment-expert` 补充专项事实和验收种子。

## 读取后必须产出

- 产品方案或 PRD 的完整结构，而不是孤立提纲。
- 待确认项及其对评审、开发、测试、上线的影响。
- 验收标准、边界路径和风险控制点。

## 需要继续读取的 reference

- 可复制模板读 `product-prd-template.md`。
- 质量门禁读 `product-prd-quality-gates.md`。
- 产品合议评审、多个 AI / PM / Reviewer / 产品大师 / MAGI 多视角审查读 `product-deliberation-workflow.md`。
- 支付资金附录由 `payment-expert` 提供。
- 运营数据发布附录读 `product-prd-operations-and-data.md`。
- 通用方法论读 `product-architecture-methodology.md`；产品洞察、资料资产化和机会雷达读 `product-insight-analyst.md`；机会清单、需求池、Backlog 决策、优先级、User Story 或 AC 场景读 `po-backlog-manager.md`。
- 支付资金和外部规则使用 `payment-expert`。

## 按任务读取索引

| 任务 | 优先读取 | 跳过 |
| --- | --- | --- |
| 用户反馈整理成 PRD | PRD 生成门禁、PRD 连环追问与原型反推，并读 `product-architecture-methodology.md` 的“反馈证据与问题地图” | 完整模板正文，除非需要交付 PRD |
| 资料洞察转产品机会或 PRD 候选 | PRD 生成门禁，并读 `product-insight-analyst.md` 的机会雷达和证据推理链 | 不从资料摘要直接生成 PRD |
| 机会清单/需求池转 Backlog、User Story 或 AC | PRD 生成门禁、用户故事写法、验收标准，并读 `po-backlog-manager.md` | 不把每个机会点都写成 P0 |
| 快速生成产品方案 | 输出格式约定、PRD 生成门禁、产品方案结构、PRD 最小必备项、验收标准 | 符合性评审细则 |
| 从零构思 / 优化已有 PRD / 既有产品增量需求 | PRD 生成门禁、PRD 连环追问与原型反推、产品语义门禁 | 不新建概念 PRD 或平行版本，不进入技术设计 |
| AI 生成方案 / 产品大师 / MAGI / 多视角合议评审 | PRD 生成门禁、符合性评审模式，并读 `product-deliberation-workflow.md` | 不把外部多 Agent 工具和平台专属流程写入 PRD |
| 原型/HTML/页面截图/交互稿反推 PRD | PRD 连环追问与原型反推、产品方案结构、页面/交互说明、验收标准 | 支付资金专项，除非命中资金场景 |
| 跨应用 / 多端原型需求规划 | 跨应用原型需求规划、产品方案结构、状态机说明、验收标准 | UI 布局、视觉和可点击原型绘制 |
| PRD 文档质量治理 / 文档过厚过薄 / 版本评审同步 | PRD 文档质量治理、PRD 生成门禁、符合性评审模式，并读 `product-prd-quality-gates.md` | 不把完整模板机械展开 |
| 完整 PRD 草案 | PRD 生成补全模式、产品方案结构、用户故事、状态机、规则矩阵、运营后台、数据报表、验收标准 | 支付资金专项，除非命中资金场景 |
| PRD 补全 | PRD 生成补全模式、PRD 最小必备项、状态机、规则矩阵、运营后台、数据报表 | 完整模板正文，除非需要重写 |
| PRD 符合性评审 | PRD 生成门禁、符合性评审模式、评审清单，并读 `product-prd-quality-gates.md` | 生成模板占位说明 |
| PRD / 需求评审会前预扫描 | 符合性评审模式，并读 `product-prd-quality-gates.md` 的“AI 预扫描四维度” | 不把 AI 疑似问题当已确认缺陷 |
| 支付资金 PRD | 通用 PRD 生成门禁与验收标准，并协同 `payment-expert` | 非资金运营细节 |
| 运营/数据/发布 PRD | 运营后台说明、数据和报表说明、验收标准，并读 `product-prd-operations-and-data.md` | 金融资金附录 |

## 0. 输出格式约定

- 文档结构优先保证可读、可扫读、可评审：先给结论或摘要，再给依据、方案、风险和待确认项。
- 正式 PRD 首屏统一使用“阅读摘要”，阅读摘要按当前结论 -> 产品定义 / 产品视图 -> 主链路 -> 核心对象与边界组织，让业务、产品、研发、测试和运营先形成同一幅产品图景；产品视图可以是短段落、列表或一张必要的图，不强制增加图形。
- 正式 PRD 按人的理解顺序展开：背景与问题、目标与非目标、定性与范围、概要设计、详细设计（业务场景与流程、能力对象、产品需求陈述）、业务规则与产品接口抽象、风险与待确认、验收摘要。完整性检查、追踪 ID 和 AI 执行控制不得抢占正文主线。
- 段落不宜过长；复杂内容优先拆成小节、列表、流程、矩阵或清单。
- 表格只用于对比、矩阵、字段、规则、状态、验收等结构化内容；表格过宽或单元格过长时，应拆成多个表或改为分节描述。
- 图形用于降低理解成本：线性场景优先编号文字，条件分支用流程图，多角色交接用泳道图，角色权限与系统边界用产品级用例图，对象生命周期用状态机；轻量 PRD 不强制画图，但必须让流程、结果和验收可读。
- 用户要求场景更直接、易懂或避免认知偏差时，优先交付“场景说明、参与者、流程、业务结果、异常处理、规则与验收”的短卡或等价最小结构；对象状态、共享规则、非目标、待确认和原型索引留在各自权威位置。图只回答当前最难理解的关系，不成为事实源；准出前让业务、产品、研发、测试和运营分别复述结果、范围、状态、规则、异常承接和完成标准，存在分歧时修正文档或图，不靠作者口头补充。
- 输入同时要求营销文案、工程实现或其它非产品交付，且明确不需要产品语义时，不先完成其中任一越界部分；按目标拆分并路由给相应专业能力后停止。只有确需稳定业务目标、对象、规则和验收时，本 Skill 才承接产品语义部分，混合写法不扩大产品专家权限。
- 正文段落和表格单元格默认使用纯文本；一般不对字段名、状态值、枚举值、示例值、角色、能力项、风险项加粗或使用代码背景。
- 只有代码标识符、接口路径、配置键、协议字段、风险等级或需要强调的结论，才使用加粗或代码背景。
- 标题层级不受此规则影响，继续使用 Markdown 标题表达文档结构。
- PRD 模板中的占位符在实际输出时应替换为业务内容，并保持普通文本展示。
- 不为了“显得正式”滥用加粗、代码背景、复杂表格和多层嵌套列表。

## 0.0 PRD 文档质量治理

高质量 PRD 不是越长越好，也不是越短越好；它应让目标读者在最短时间内理解为什么做、做什么、不做什么、如何验收、变更影响谁。

- 先定文档目标和读者：研发看范围、接口、规则和验收；测试看场景、边界、数据和验收样例；运营看流程、后台、SOP 和推广影响；业务或管理者看价值、成本、风险和决策点。
- 按角色分层消费：决策层回答 Why / What 和成功标准，方案层回答 How、取舍、成本和风险，实现层回答输入、输出、边界、异常、接口和验收；不同读者只展开其完成判断所需的层级。
- 区分文档类型：PRD 说明要做什么和如何验收；MRD 说明市场、客户机会和为什么做；BRD 说明商业价值、投入产出和决策依据。不要把三类材料混成一份难以快速评审的长文。
- 控制文档深度：复杂业务拆成主文档和附录。主文档保留背景、目标、定性、范围、概要与详细设计、流程、规则、接口抽象、风险和验收摘要；重复字段、完整追踪矩阵、运营/数据/支付专项细节按需放附录。
- 按需补核心概念和业务抽象：术语存在歧义、新业务对象首次进入 PRD、多主体协作、平台化能力或工程交接会受影响时，先统一概念、抽象目的、边界、反例、不变量和验收追踪；轻量 PRD 不强制展开。
- 面向工程交接的 PRD 应补充可被代码和测试消费的领域术语、规则名、状态名和异常名；不要让研发只能用注释解释未命名的业务规则。
- 产品文档描述目标、主体、能力、对象、状态、业务阶段、可观察输入输出、规则、异常、人工兜底、业务红线和验收；同步 / 异步反馈、时限和一致性等只有会改变用户或上下游承诺时才作为产品约束。
- `Handler`、事务边界、MQ、Outbox、Saga、Repository、实现类和中间件选型归入系分设计或实现 Spec。已有技术事实只有在构成已确认外部依赖或产品限制时才可作为约束引用，不得写成产品方案。
- 控制图形数量：每个场景优先选择最能减少误解的一种主表达；只有不同图分别解决独立评审问题时才组合，不为显得完整同时塞入用例图、流程图、泳道图、状态机和能力地图。
- 防止过薄：功能名不能替代需求。至少补齐范围边界、功能入口、流程交互、业务规则、验收标准和参考场景样例。
- 保持活文档：需求变更必须更新版本号、影响范围、通知对象、当前评审结论和过程记录链接；详细变更原因、讨论过程、修订流水和被拒方案放入任务计划、评审报告或 Decision Log。文档、原型、规则矩阵、验收标准和工程交接口径不能互相冲突。
- 需求变更必须生成影响清单，至少检查目标/范围、流程/交互、规则/字段、接口/事件、验收种子、测试用例、发布风险和通知对象；只改正文、不更新交接物会制造文档腐化。
- 复杂变更优先写 Delta：`ADDED / MODIFIED / REMOVED` 分别说明新增、修改和移除的产品事实、规则、流程、验收种子和工程交接影响，不重写整份 PRD。
- 最终 PRD 不保留讨论过程、迭代草稿、AI prompt/回答轨迹、多角色争论或被拒方案展开；这些内容只作为过程证据链接回正式正文。

## 0.A 业务表达、产品需求与业务规则契约

本节是 Business Expression Contract、Requirement Statement Contract 和 Business Rule Contract 的唯一详细权威。`product-prd-template.md` 只投影填写槽位，方法论和质量门禁只说明何时使用与如何评审，不维护平行字段。

先正名六类内容：

| 名相 | 回答的问题 | 不得混入 |
| --- | --- | --- |
| 业务表达 | 事实是什么，问题为何成立，影响谁，产品建议什么 | 未确认事实、实现细节、营销口号 |
| 产品需求 | 在什么条件下，哪个主体必须得到什么业务行为或结果 | 规则来源推断、技术实现方案 |
| 业务规则 | 业务活动必须遵守什么约束、许可、禁止或推导 | 流程顺序、执行代码、规则引擎设计 |
| 业务流程 | 参与者按什么顺序行动和交接 | 隐藏在步骤里的未命名规则 |
| 执行机制 | 系统如何实现、存储、传输和恢复 | 产品专家提前设计的内部方案 |
| 验收证据 | 如何证明需求和规则已满足 | “测试通过”等无证结论 |

### Business Expression Contract

业务表达用于把访谈、会议纪要、用户反馈、原型、历史 PRD 和运营材料转成目标读者能够作出判断的产品语言。最小字段：

```text
目标读者 / 需要作出的判断：
已确认事实 / 来源：
真实问题 / 影响对象 / 代价：
产品判断 / 推荐方案 / 适用边界：
本期承诺 / 非目标：
推断 / 待确认 / Owner / 未确认前处理：
```

表达时遵守：

- 事实先于判断，判断先于决策，决策先于承诺和验收。
- 优先使用主动责任句：“谁在什么条件下做什么，产品产生什么可观察结果，失败后谁承接”。
- 不用“进行处理”“相关方负责”“按相关规则”“视情况”“及时”“合理”等表达隐藏主体、条件、尺度或责任。
- 技术问题先翻译成业务问题。例如，不先问“幂等键是什么”，而问“同一请求重复提交时，应新建、拒绝还是返回原结果”。
- 业务表达服务产品语义，不承担营销文案、品牌文案或正式报告润色。

详细业务说明优先使用同一契约的四段式投影，不另建平行事实源：

```text
业务情境：谁因为什么问题，在什么状态下要取得什么业务结果。
责任推进：谁先做什么，产品依据什么继续，谁接收反馈或承接异常。
裁决与状态：适用什么规则，对象从什么状态变为什么状态。
异常与收口：失败后看到什么、谁接管、怎样恢复、什么证据证明完成。
```

四段式服务连续阅读；需要跨章节追踪、规则审计或复杂参与者交接时，再展开场景 ID、需求陈述、规则卡和验收摘要。不得同时用叙述段、场景卡、独立流程和验收段重复同一事实。

### Requirement Statement Contract

一条产品需求只表达一个外部可见业务事实、质量要求或约束。最小字段：

```text
需求名称：
需求类型：功能 / 质量 / 约束
责任主体：
场景 / 前置状态：
规范强度：必须 / 不得 / 应 / 可
要求的行为或业务结果：
度量、时限或边界：
来源与可靠性：
关联规则：
验收样例：
```

规范强度：

| 术语 | 含义 | 使用约束 |
| --- | --- | --- |
| 必须 | 绝对要求 | 不满足即需求失败 |
| 不得 | 绝对禁止 | 出现即触发失败或红线 |
| 应 | 默认要求 | 偏离必须说明理由、影响和批准方 |
| 可 | 允许但非必需 | 不得被研发或测试解释为必须 |
| 建议 | 非规范性指导 | 不进入正式需求或验收判定 |

需求质量按价值和风险裁剪。P0/P1、跨团队、外部契约或高风险需求必须同时检查：

- 适切：描述真实且已同意的利益相关者需要。
- 必要：能回到问题、目标、风险或外部约束。
- 原子：一个主体、一个主要谓词、一个可判定结果。
- 无歧义：主体、条件、范围、单位、时区、次数和例外只有一种合理解释。
- 自包含：理解本条需求不依赖作者口头补充。
- 可理解：业务、产品、研发和测试共享同一含义。
- 可验证：存在可观察结果、度量或人工确认方式。
- 可追踪：能回到来源，并向前连接场景、规则和验收。

EARS 只作为复杂状态、事件、可选条件和异常行为的可选受控表达，不要求所有需求使用固定英文句式，也不把 EARS 形式完整等同于需求正确。

### Business Rule Contract

业务规则是基于业务术语和事实，对业务行为施加的声明式约束、许可、禁止或推导。规则独立于流程和执行机制；流程引用规则，执行机制实现或执行规则。

简单规则最小字段：

```text
规则名称：
规则性质：不变量 / 场景裁决 / 派生计算 / 版本化或外部规则
业务动机：
适用对象与范围：
输入事实：
当：
则：
Owner：
正例 / 边界例 / 反例：
```

多条规则可能同时命中、规则存在优先级或聚合结果、规则来自外部版本、规则影响高风险结果或例外会改变结论时，增加：

```text
规则编号：
允许 / 禁止 / 必须：
状态变化 / 业务副作用：
优先级 / 命中策略：
例外规则引用：
异常 / 外部不可用 / 人工兜底：
来源：
版本：
生效期 / 适用法域或主体：
未确认前处理：
审批 / 灰度 / 回滚 / 历史追溯：
```

多输入、多输出或规则重叠时使用决策表。决策表至少声明输入、输出、规则行、命中策略，以及默认结果或明确的无匹配处理；不为简单规则强制使用 DMN 或规则引擎。

例外必须写成可追踪规则或明确的人工裁决门禁，不写“特殊情况另行处理”。

### Rule-Example-Question Mapping

需求或规则存在歧义、高风险或跨团队争议时，用最小映射澄清：

```text
业务场景 / 需求：
规则：
正例：
边界例：
反例 / 禁止结果：
未决问题 / Owner / 未确认前处理：
```

该映射只生成澄清结论和验收种子，不要求安装 Cucumber、采用 Gherkin 或新建过程文档。正式 PRD 只吸收已确认规则和必要例子。

## 0.B 产品定性与概念投影

产品定性先拆开三条正交轴，不能放进一个混合枚举：定性对象回答“当前讨论的是什么”，本期变化回答“这次改变什么”，文档强度回答“PRD 需要写多深”。产品、系统、平台、服务等名称后缀不能证明对象性质，页面数量、开发量和产品类型也不能直接决定文档强度。

### 定性对象

优先使用以下语义类型：产品、商业供给、产品能力、业务主体、业务对象、业务流程、业务规则、业务状态、业务指标、交互载体、协议、技术机制。某个概念只有具备明确目标主体、独立价值、责任 Owner、服务边界、持续生命周期和成功口径时，才可定性为产品或独立商业供给；证据不足时降为候选、待确认或其它语义类型，不从名称推断。

### 本期变化与文档强度

- 本期变化使用新建、增强、治理、风险约束、运营提效、迁移、退役或验证；平台是责任层级，不是变化类型。
- 轻量适用于低风险、可逆、单一责任和单一主链路；标准适用于多主体、对象、状态、团队或公共契约；增强适用于资金、合规、安全、隐私、外部规则、不可逆或高失败成本。
- 高风险小改动仍用增强 PRD；新产品的早期可逆验证也可以先使用轻量文档。轻量只裁剪表达深度，不删除权限、主要失败、人工责任、审计和可观察验收。

### 术语、概念与系统别名

- 术语是概念的名称；业务概念是团队需要共同遵守的业务含义；系统别名是代码、字段、接口或历史系统的称呼。
- 同一概念可以有多个术语和系统别名，同一术语也可能在不同业务域代表不同概念；当前代码、页面或数据库不能自动成为业务定义。
- PRD 只投影会改变本期主体、对象、状态、规则、指标、责任或验收的核心概念，并记录语义类型、定义、边界或不等于、当前状态、Owner 与权威来源。其它术语引用当前有效术语库，不复制全文。
- 概念含义、边界、状态或关系变化属于产品语义变化，应升级 PRD 版本并检查场景、规则、数据、验收和工程交接影响；只修改译名或系统别名且概念含义不变时，只更新术语权威。
- 已废弃概念不得因旧页面或代码仍存在而恢复为当前产品事实；只有迁移或退役本身是当前目标，且 Owner、期限、影响、退出条件和验收明确时，才进入 PRD。

正式 PRD 先运行 `check_product_qualification.py` 检查可发现字段和概念表，再运行 `check_product_deliverable.py --kind prd` 检查整体结构。两者都不能判断产品分类、术语事实或业务结论是否正确，仍需概念 Owner、业务 Owner 和目标读者复述确认。

## 0.1 PRD 生成门禁

生成或评审 PRD 前，先按 `product-scenario-routing.md` 的产品语义门禁检查。信息不足时仍可产出草案，但必须把不确定内容放入“待确认项”，并说明影响范围。

当输入主要来自用户反馈、工单、销售转述、会议纪要或“希望加某功能”的需求列表时，先建立反馈到问题的证据链。功能请求不是问题定义；PRD 必须说明原始反馈、真实问题、使用场景、影响范围、证据强度和潜在机会，或明确这些信息仍待确认。

当输入主要来自资料、访谈、竞品、行业/政策、标杆实践或知识库笔记时，先读取 `product-insight-analyst.md` 形成机会雷达，说明资料范围、现状证据、矛盾/变化、产品机会、预期价值、证据来源、置信度、待验证项和建议去向；不要从资料摘要直接写功能范围或 PRD 正文。

进入 PRD 正文前，应先形成轻量需求分析结论。若缺少根源需求、产品定义、产品边界、稳定点 / 变化点或上下游分工，PRD 可以输出草案，但必须把这些缺口集中放入“待确认项”，不能把用户提出的功能解法直接写成已确认范围。进入正式评审或工程交接前，还要检查需求条目标准：一个条目只表达一个外部可见业务事实、规则事实或验收事实；P0/P1 条目必须可验证、可追踪、无二义性且不绑定内部实现。系统需求、外部规则或业务 owner 未确认时，只能列缺口、影响和确认方，不把软件侧推断写成最终 PRD。

正式、完整、可评审、提交前、CR 或触发验证场景中，PRD 先运行 `scripts/check_product_qualification.py`，再运行 `scripts/check_product_deliverable.py --kind prd`；产品架构方案运行 `scripts/check_product_deliverable.py --kind product-architecture`。脚本只检查可发现结构，不联网、不写文件、不替代产品定性、概念事实、业务判断或合规审查；无法运行时必须说明原因、人工检查结果和残余风险。

最低门禁：

- 原始诉求已转换为根源需求，或明确哪些部分仍是待验证假设。
- 产品定义说明对象、能力、可见价值行为和成功口径，而不只是功能名或页面控件。
- 产品边界说明本产品负责、合作方 / 外部系统负责、本期不做和人工/运营承接部分。
- 稳定点 / 变化点带有边界坐标；没有证据的未来变化不作为平台化、扩展点或系统拆分依据。
- 目标和非目标可验证。
- 用户、主体、角色、风险承担方和验收方不混用。
- 核心概念、业务抽象、核心对象、状态、字段口径和不变量能支撑流程；若没有概念歧义或抽象需求，应能说明不展开的裁剪理由。
- 主流程、逆向流程、异常流程、人工流程和运营兜底至少有草案。
- 复杂流程能按评审问题选择流程图、泳道图、状态机、用例图或能力地图之一辅助阅读，或明确说明为何编号文字已经足够；图不是新的事实源。
- 图、原型和表格必须能回到正文、需求 ID、规则编号或验收种子；不得只用图表达未确认的业务结论。
- 规则有触发条件、判断逻辑、优先级、版本和验收样例。
- 数据指标、报表、埋点、审计和导出口径有负责人或待确认方。
- 涉及资金、合规、隐私、安全、外部规则时，必须标注专业确认方，不写成最终合规结论。

待确认项建议至少包含：问题、影响章节、风险等级、确认方、未确认前的默认处理、截止时间或进入研发前门禁。

### 产品需求切片与实现后语义验收

跨应用、跨模块或需要多轮交接的复杂需求，按可独立验收的产品语义拆片。产品需求切片固定为：`需求 ID / 业务身份 / 目标与非目标 / 对象与状态 / 规则与业务事件 / 正常、边界、异常与人工兜底 / AC / 待确认 Owner`。

- 产品专家只定义外部可见的产品事实、规则和验收，不设计内部接口、模型或表。
- 实现后消费 Requirement-Diff 对账：对 `done / partial / todo` 逐项做产品语义验收；发现偏差时先冻结受影响切片，由产品 Owner 裁决产品契约变化还是实现偏离，再决定是否标记 `changed`；`blocked` 保持 PENDING 并停止受影响切片。
- 产品变化先更新 PRD、Delta 和 AC，再通知下游做影响识别；不能把当前代码自动升级为产品事实。知识回流只写业务术语、规则版本和验收样例，不写工程实现细节。

## 0.2 PRD 生成、补全与符合性评审模式

PRD 任务先判断使用者真正要的是生成、补全还是评审：

- 生成：用户给出产品或功能主题，并说明目标、问题或业务背景时，可以直接产出可评审草案；缺失的信息放入“待确认项”，不要因为细节不足只输出空模板。
- 补全：用户给出已有 PRD、片段或章节时，保留原有口径和结构，补齐目标、范围、角色、对象、流程、规则、数据、风险和验收；无法确认的内容标注待确认，不擅自写成事实。
- 符合性评审：用户要求审 PRD、CR、提交前检查或判断是否可交付时，输出“符合项 / 必改 / 建议 / 可选”，每条都写清章节或位置、问题、影响和建议改法。

当用户同时缺少产品或功能主题，也缺少目标、问题陈述或目标用户时，先追问 1-3 个关键问题：产品或功能名称、要解决的问题、目标用户、核心目标或成功指标。只要已有主题和目标，就可以先生成草案，并用待确认项或待确认问题表承接不确定内容。

符合性评审至少检查：

- 是否需要评审会前 AI 预扫描：完整性、一致性、可测试性、二义性四维度只产出疑似问题和追问点，不能替代正式评审或 owner 决策。
- 多视角或合议评审是否有触发原因、阶段门、共识、分歧、必改、建议、待确认、owner 和验证方式，避免只给观点或角色表演。
- 必备章节是否缺失：背景与问题、目标与成功指标、用户与角色、范围与非目标、功能需求、业务规则、依赖约束和验收标准。
- 成功指标是否可衡量：避免只写“提升体验”“优化效率”，应补成指标口径、基线、目标值、观察窗口和负责人。
- 用户故事是否包含角色、能力和价值：不能只有“用户需要某功能”，必须说明谁、在什么场景、完成什么能力、获得什么业务价值。
- 功能需求是否可测试：每条需求应有需求 ID、优先级、前置条件、输入、处理、输出、异常和验收样例。
- 洞察是否有证据与推理链：资料、访谈、竞品或标杆材料应先形成机会雷达，说明现状证据、矛盾/变化、产品机会、预期价值、证据来源、置信度和待验证项，不能从资料摘要直接写功能范围。
- Backlog 优先级是否有依据：P0/P1/P2 应能追溯到 BV/EE、业务/用户/工程三桌校验、拒绝或延后理由，不能把老板关注、竞品已有或客户提过直接写成 P0。
- 范围与非目标是否能防止范围扩散：本期不做什么、依赖什么、人工兜底什么必须可判断。
- 验收标准是否能回到需求 ID、流程、规则、状态或数据指标，不能只有泛化的“测试通过”。
- 依赖、风险、外部规则、合规、隐私、安全、支付资金和专业确认方是否被标注。

分级口径：

- 符合项：已经满足模板、可验收性或专项门禁的内容，写明依据章节。
- 必改：缺少必备章节、成功指标不可衡量、需求不可测试、范围边界不清、验收标准无法追溯、强监管或资金相关内容缺少确认方。
- 建议：不阻断评审但会降低协作效率的结构、表达、示例、流程图、规则矩阵、术语一致性或数据口径问题。
- 可选：不影响当前交付的表达润色、附录补充、后续版本能力和更精细的运营增强。

正式评审时，依次运行 `scripts/check_product_qualification.py` 和 `scripts/check_product_deliverable.py --kind prd`，把脚本发现的结构缺口合并进“必改”；脚本未发现问题也不代表业务、合规、风险或验收已经通过。

## 0.3 PRD 连环追问与原型反推

PRD 是产品思考结构，不只是文档模板。面对模糊需求，先用连续追问暴露目标、主体、对象、流程、规则、数据和风险缺口，再决定是先出草案还是先补关键事实。

先识别输入模式，模式只决定本轮如何收敛，不建立新的事实源：

- 从零构思：先按用户、业务和可行性约束形成 `product-architecture-methodology.md` 中的需求分析结论卡；owner 确认根源需求、产品定义、目标用户、MVP、非目标和验收种子后，再进入 PRD-Lite 或正式 PRD。
- 优化已有 PRD：以现有权威文档为主版本，先诊断目标、范围、非目标和产品判断，再补对象、流程、规则、状态和验收缺口；不因追求完整机械重写全文。
- 既有产品增量需求：复用当前 PRD 和已确认产品上下文，只写受影响产品事实的 `ADDED / MODIFIED / REMOVED` 与影响清单，不重写整份 PRD，也不按固定功能批次扩需求。

需求分析结论卡是进入正式产品文档前的确认载体，不另建“概念 PRD”或第二套模板。正式 PRD 承载产品语义；技术栈、数据库模型和 HTTP / RPC 接口进入系分，Agent 禁行规则、执行门禁和验证命令进入 Spec / Harness 或 `wise-agent` 的工程控制资产。

低摩擦追问纪律：

- 一次只关闭一个关键 blocker；按用户、业务、可行性约束组织问题，但不机械轮询三个视角。
- 可枚举的问题给 2-3 个选项和建议答案；可从材料合理推断的信息先给默认假设、依据与影响，再请 owner 确认，不把选择题全部抛回用户。
- 普通低风险场景中，同一问题轴连续 2-3 轮仍未收敛时，给出推荐答案、假设边界和停止条件；资金、合规、安全、隐私、公共契约等高风险事项没有 owner 确认时继续保持 PENDING，不用默认值越过门禁。
- 问询过程只进入任务计划或评审记录；正式 PRD 只吸收已确认事实、合理假设、待确认项和最终取舍。

用户反馈输入处理：

- 先把原始反馈、真实问题、使用场景、影响范围、证据强度和潜在机会整理成问题地图，再决定是否进入 PRD。
- 对“用户需要某功能”的表述，追问其任务阻塞、当前替代方式、失败成本、频率、影响对象和可验证信号。
- AI 初稿只能作为聚类和表格化辅助；交付前必须人工检查是否过早给出方案、忽略证据强度或合并了不同场景的问题。
- 如果问题价值不清，先输出验证动作、待确认问题表或非目标说明，不把低证据反馈包装成已确认需求。

模糊需求处理：

- 先判断是否已有产品或功能主题、目标、用户/主体和成功指标；缺少两项以上时，先输出“待确认问题表”，不要直接堆模板。
- 待确认问题表字段建议包含：问题 ID、待确认问题、影响章节、风险等级、建议选项、未确认前处理。
- 问题 ID 用 `Q-01` 递增；每个问题写清影响的是目标、范围、流程、规则、数据、验收还是风险。
- 追问优先覆盖主体与责任、核心对象、主/逆/异常流程、规则版本、数据口径、风险确认方和验收方式。
- 已有主题和目标时可以先生成草案，但缺失处必须落到待确认项，不得把假设写成事实。

原型、HTML、页面截图、交互稿或页面说明反推 PRD：

- 先从页面、字段、按钮、状态和交互反推出角色、对象、流程、权限、规则、异常、数据指标和验收标准。
- 输出 PRD 时按产品语义组织，不只描述页面控件；页面说明必须服务业务对象、用户任务和可验收行为。
- 错误截图、日志截图、测试失败截图不是本节的输入材料；如果目标是定位根因、修复代码或补回归测试，应转交 `资深架构师`。
- 若原型展示的是支付、资金、VCC、账户账本、清结算、对账或跨境能力，继续读取支付资金专项 reference。
- 无法从原型确认的后台规则、风控、运营兜底、数据口径和外部依赖，统一进入待确认问题表。

## 0.4 跨应用原型需求规划

当一个需求同时涉及运营平台、C 端或其他应用，以及浏览器、桌面客户端、H5 等客户端形态时，先规划产品语义落点，再进入 UI 绘制。目标不是为每个端复制一套页面，而是让端到端任务、业务责任和验收可追踪。若同时出现 PC、手机、浏览器、APP、H5、响应式、触控、键盘、弱网或中断恢复，先读取 `product-client-interaction.md`，把设备情境与运行载体拆开，再回到本节完成承接。

### 应用、客户端、页面与非页面能力

- 应用：承担一组业务责任和用户任务的产品边界，例如运营平台、商户端或 C 端；它不是服务、代码模块或部署单元。
- 客户端：承载交互和运行约束的形态，例如桌面 Web、移动 Web / H5、桌面客户端或原生移动端；运营端 / C 端本身不等于客户端类型，桌面客户端还要确认使用 Web 容器还是原生 UI。
- 页面：某个角色在特定应用和客户端中完成任务、观察状态或恢复失败的交互载体。
- 非页面能力：通知、定时任务、导出、外部回调或无需用户进入页面的业务动作；不得为了覆盖需求而强行造页面。

### 最小交付

1. 需求覆盖矩阵：`需求 ID | AC ID | 角色 | 应用 | 客户端 | 状态`。沿用项目已有稳定 ID 体系，不另造第二套编号。
2. 应用界面基线：`应用 | 客户端 | 设计系统姿态 | 继承 / 参考来源 | 品牌与密度边界 | Owner | 例外处理`。姿态只描述完整采用、继承参考、独立体系或待确认；产品专家不在此定义组件样式、token 值或实现 API。
3. 原型载体清单（页面清单）：`承接 ID | 类型 | 名称 | 应用 | 客户端 | Owner`。类型只区分页面、页面状态和非页面能力；每个载体使用稳定且唯一的承接 ID。
4. 产品级页面标注：`标注 ID | 承接 ID | 业务对象 / 字段或内容 | 前置条件 / 规则 | 权限 | 动作与状态变化 | 必须反馈的结果 / 异常 | 数据来源 / 时效 | 埋点 / 审计 | AC`。`标注 ID` 稳定且唯一，是 HTML、Figma 或其他审阅载体引用产品事实的键；它只定义必须呈现、可操作和可观察的产品事实，不规定布局、尺寸、token 或组件样式。
5. 多端差异矩阵：先列跨端不变的对象、规则、状态和验收，再记录由设备能力、输入方式、会话、网络、通知或使用情境造成的差异、证据、影响和回退；没有证据的差异不复制需求。
6. 跨应用衔接：说明身份 / 会话、入口与跳转、上下文携带、任务 Owner、状态同步、通知、失败重试 / 恢复和审计责任。
7. 原型覆盖追踪：`需求 ID | AC ID | 承接 ID | 关系姿态 | 覆盖状态 | Owner | 未覆盖前处理`。一行只表达一个关系，姿态使用 `required / target / informational / optional`；`required` 必须已覆盖，其他未覆盖关系必须给出 Owner 与进入下一阶段前的处理条件。每条需求和 AC 必须映射到页面、页面状态或明确命名的非页面能力；无需求来源的载体、无载体承接的需求和不可验收状态都要删除或显式闭合。
8. 客户端交互契约：`应用 | 设备情境 | 运行载体 | 输入 | 系统/网络约束 | 导航 | 反馈与恢复 | 证据 | Owner`，并附每端主任务与失败恢复验收卡；“好用”必须落到可观察行为，不以静态截图代替走查。

### HTML 原型标注交付契约

原型明确以自包含 HTML 交付时，在范围规划中声明 `原型交付形态：standalone-html`，并增加“HTML 原型标注契约”。产品专家只定义审阅所需的产品事实与追踪关系；`ui-design-expert` 负责标记、面板、响应式和可访问性表达，浏览器代码与运行验证仍交 `senior-software-architect`。

- HTML 默认进入 `experience` 体验模式，不显示标注；审阅入口可使用查询参数或显式开关。规划先填写非空 `原型修订`，再使用规范值：`默认模式=experience`、`审阅入口=review-query-or-toggle`、`标注数据源=embedded-json`、`模式隔离=preserve-task-state`、`可访问性=keyboard-and-name`。
- 标注数据与原型使用同一 revision，轻量独立原型优先把结构化 JSON 内嵌在 HTML；不另建服务、框架或平行标注文档。标注多到影响单文件维护时，再按 UI 原型变更契约判断是否升级载体。
- 标注锚定稳定语义元素，产品规划中的 HTML 锚点规范写成 `#element-id` 或 `[data-name="value"]`，不使用像素坐标、DOM 层级或 `nth-child`。标记必须可键盘聚焦、有可访问名称且不能只靠颜色区分；窄屏下使用不遮挡主任务的抽屉或等价容器。
- 标注类型只按当前事实裁剪使用 `scope / content / rule / interaction / trace`；事实状态使用 `confirmed / inferred / pending`，不得把推断或待确认内容写成已确认事实。
- HTML 标注表使用 `标注 ID | 承接 ID | HTML 锚点 | 类型 | 事实状态 | 需求 ID | AC ID | Owner`，其中 `标注 ID` 必须引用产品级页面标注的同名事实，承接 ID 与 AC 必须一致。每个已覆盖的 `required` 页面或页面状态至少有一条标注；非页面能力继续留在原型覆盖追踪，不得进入 HTML 标注表。

产品级页面标注仍是业务事实权威，HTML 标注表只负责把事实稳定绑定到可审阅载体，不复制布局、尺寸、token、组件 API 或开发注释。最终自包含 HTML 还必须内嵌 `id=prototype-annotations`、`type=application/json` 的数据块，包含非空 `revision` 以及 `id / target / content / status`；标注 ID 唯一、内容非空、状态只取 `confirmed / inferred / pending`，目标必须能解析到实际元素。

正式、完整、可评审、提交前、CR 或触发验证场景中，先人工确认应用界面基线，再运行 `scripts/check_product_deliverable.py --kind prototype-scope-plan --file <原型范围规划.md>`。脚本检查三张追踪表、产品事实引用、规范值、稳定锚点及页面 / 页面状态的必需覆盖。最终 HTML 另运行 `python3 ui-design-expert/scripts/check_ui_source.py --require-prototype-annotations <prototype.html>`，检查真实文件中的默认模式、审阅入口、内嵌数据、内容和锚点解析。两项通过仍只证明静态契约与载体完整，不代表产品语义、交互隔离、可访问性或浏览器任务已经验收。

### 经世校准与协作边界

用户明确要求华夏经世智慧校准时，加载 `huaxia-practical-wisdom`，只用它补四个审查问题：应用、客户端、页面之名是否符合实际责任；局部页面是否闭合端到端任务；跨端共性与有证据的差异是否兼顾；每项规划是否落实为可验证结果和反馈。经典镜片不替代用户事实、产品 Owner 或验收。

产品专家决定需求落在哪个应用、客户端、页面或非页面能力，必须呈现、可操作和可观察什么，并声明各应用 / 客户端是完整采用、继承参考、独立体系还是待确认；涉及多端交互时还要持有端形态、输入、导航、反馈、恢复和验收契约。`ui-design-expert` 决定 Web 信息架构、布局、组件、tokens、交互细节、响应式、可访问性和 L0 / L1 / L2 原型表达。非 Web 客户端按真实运行形态交对应平台能力；当前没有该能力时止于产品规划，不假装完成界面设计。浏览器代码和工程验证交 `senior-software-architect`。应用 / 客户端范围不清时标为待确认，不用原型替 Owner 做业务决策。

## 1. 产品方案结构

```text
1. 背景与问题
2. 目标与非目标
3. 定性、范围与总体判断（名实、范围、架构、取舍）
4. 概要设计
5. 详细设计（业务场景与流程、能力对象、产品需求陈述）
6. 业务规则与产品接口抽象
7. 数据、权限、风险与待确认
8. 验收摘要
9. 按需附录 / 执行计划引用
```

这是一条读者主线，不是机械必填目录。标准 / 增强 PRD 的阅读摘要先让读者复述当前结论、产品架构主脊、本期边界、主链路 / 异常承接和关键待确认；轻量需求可以合并相邻章节，复杂需求再展开对象、状态、能力地图、运营后台、数据指标和专项附录。详细验收矩阵、AC 与测试映射、验证命令、执行 owner 和任务状态进入实现 Spec 或执行计划；PRD 只保留产品 owner 能理解和确认的业务结果、关键边界与红线。

## 2. PRD 最小必备项

- **背景**：为什么做，当前痛点是什么，影响谁。
- **目标**：业务目标、用户目标、数据目标和交付目标。
- **范围**：本期做什么，不做什么，依赖什么。
- **角色**：前台用户、后台用户、外部参与方、系统参与方。
- **业务场景与流程**：标准 / 增强 PRD 的 P0/P1 场景使用业务情境、责任推进、裁决与状态、异常与收口形成可直接复述的短闭环，不再另设同义的场景内流程。存在多个场景时先声明场景关系为串联、并行、分支、互斥或独立；独立场景分别闭环，关联场景再用一段编号流程或一张关键图表达跨场景交接和共享状态。对象状态、共享规则、非目标、待确认和原型索引在各自权威位置维护并由场景引用；页面、按钮、CRUD、API 或笼统功能名不是业务场景。
- **功能**：功能列表不能孤立存在，必须绑定角色、场景和流程；每个功能都应说明它是哪个对象对外提供的可见价值行为，来源于哪个目标或父能力。
- **核心概念/业务抽象**：按需定义关键术语、抽象目的、适用范围、反例、主体责任、关系、不变量和验收追踪，避免不同团队用同一个词表达不同对象或规则。
- **领域命名**：对下游建模、代码命名和测试命名有影响的对象、状态、规则、异常和人工兜底，应给出统一中文术语、英文/代码命名建议或待确认命名。
- **对象**：对象定义、字段口径、生命周期、状态和关系。
- **规则**：权限、审批、计费、额度、风控、通知、报表、数据留存。
- **异常**：失败、撤销、超时、重复、并发、数据不一致、人工兜底。
- **数据**：指标、埋点、报表、导出、审计和追溯。
- **验收**：正文末尾概括业务结果、关键边界、异常和红线；详细场景、输入输出、测试映射与执行状态进入执行计划或按需附录。

## 3. 用户故事写法

用户故事必须有上下文和验收，不只写“作为用户，我想要”。

```text
优先级：<P0/P1/P2；依据 BV/EE 和三桌校验>
场景：<用户在什么业务背景下>
角色：<谁>
目标：<想完成什么>
价值：<为什么现在值得做；不做会损失什么>
前置条件：<权限、状态、数据、外部依赖>
触发动作：<用户或系统做了什么>
系统响应：<产品应该如何处理>
异常分支：<失败、超时、重复、权限不足>
依赖/风险：<数据、系统、合规、运营或工程确认方>
验收标准：<可测试的结果>
```

从机会点转 User Story 时，先确认三件事：

- 它不是单纯“竞品有”“老板说”“客户提过”，而是有明确角色、场景和价值。
- 它已通过 BV/EE 初判；工程代价不清时先标待确认或交给架构师评估，不直接承诺 P0。
- 它的 AC 能覆盖输入、输出、正常、边界、异常、不能接受情况和完成标准。

User Story 可作为轻量需求的场景表达；当同一功能因主体、前置状态、规则裁决或异常收口不同而产生多条业务路径时，应拆成业务场景与场景内流程，并声明场景关系，不重复维护两套正文。

## 4. 状态机说明

每个状态机至少说明：

| 状态 | 含义 | 进入条件 | 可执行动作 | 退出条件 | 是否终态 | 审计要求 |
| --- | --- | --- | --- | --- | --- | --- |

状态机红线：

- 不允许多个含义混在一个状态里。
- 不允许只有成功路径，没有失败、取消、超时和人工处理状态。
- 不允许状态变化没有触发事件、操作者、时间和来源。
- 不允许终态还能被普通操作随意改写。

## 5. 规则矩阵说明

适用于额度、计费、审批、风控、通知、权限、数据留存等规则。

| 规则编号 | 规则名称 | 规则性质 | 适用场景 / 步骤 | 适用对象 | 触发条件 | 判断逻辑 | 动作 | 优先级 | 版本 | 验收样例 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |

规则需要补充：

- 是否可配置。
- 谁可以配置。
- 是否需要审批和复核。
- 是否支持灰度、回滚和历史版本。
- 是否影响历史数据和报表口径。
- 跨场景不变量集中定义并由场景引用；场景裁决规则必须落到具体场景或步骤；版本化 / 外部规则补充来源、owner、生效范围和未确认前处理。

## 6. 运营后台说明

运营后台不是“管理端页面”，而是业务闭环的一部分。

必须说明：

- 查询条件和结果字段。
- 审核、复核、驳回、关闭、补录、导出等动作。
- 权限、职责分离、敏感字段脱敏和操作日志。
- 人工处理 SLA、工单流转、告警和升级机制。
- 修改前后值、规则版本、操作者、来源和审计留存。

## 7. 数据和报表说明

- 指标定义必须有口径、维度、时间范围、数据源和负责人。
- 报表必须能追溯到明细，不依赖无法解释的汇总数。
- 埋点要说明事件名、触发时机、属性、去重和数据质量校验。
- 导出和查看需要权限控制、脱敏、操作审计和留存策略。

## 8. 验收标准

验收标准建议按以下类型组织：

- 正常路径：用户能完成目标动作。
- 边界路径：空值、极值、重复、并发、权限边界。
- 异常路径：失败、超时、外部依赖不可用、数据不一致。
- 运营路径：审核、复核、补录、导出、人工关闭。
- 数据路径：指标、报表、审计、日志和追溯正确。
- 风险路径：合规、隐私、安全、资金、审批和外部规则待确认项。

## 9. 评审清单

- 目标是否清楚，是否能被指标或验收标准验证。
- 用户、主体、角色和权限是否完整。
- 功能/能力是否来自目标、父能力或业务对象分解，而不是按钮、页面或头脑风暴清单。
- 核心对象、状态机和规则是否能解释所有流程。
- 主流程、逆向流程、异常流程和人工流程是否闭环。
- 运营后台是否具备查询、处理、复核、审计和告警能力。
- 指标、报表和埋点是否有明确口径和数据源。
- 是否列出依赖、风险、待确认项和非目标。
- 涉及支付、资金、合规、隐私、安全时是否进入专项审查。
