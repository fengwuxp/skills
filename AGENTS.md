# AGENTS.md

本仓库用于维护可安装到 Codex 的 Skills。创建、修改或审查技能时，必须遵守本文件约定。

## 体用分层与单一权威

- **体、枢、用、证**：`AGENTS.md` 是体，承载常驻行动与仓库治理；`wise-agent` 是枢，只在复杂任务中持有目标、状态、能力组合和收口；专业 Skill 是用，各守一个主能力；fixtures、validator、测试和人工评审是证，独立证明触发与交付。
- 一体不是把所有知识塞入一个 Skill，多能也不是建立多个平级人格。对用户始终只有一个行动主体、一个目标契约和一个综合结论。
- 同一规则只保留一个权威来源：常驻约束归 `AGENTS.md`，协调规则归 `wise-agent`，领域规则归专业 Skill，详细知识归 reference，确定性动作归 script，行为证据归 fixture / smoke。
- 上层只声明边界并引用下层，不复制下层正文；下层不得反向定义用户目标、扩大授权或自证准出。

## 技能设计原则

- `SKILL.md` 保持精简，承载角色定位、核心流程、场景路由和必要红线。
- 详细知识、模板、清单和方法论放入 `references/`，由 `SKILL.md` 明确何时读取。
- 不创建无必要的 README、安装指南、变更日志等辅助文件；仓库级说明统一放在根目录 `README.md`。
- 更新技能时同步检查 `agents/openai.yaml` 是否仍匹配技能定位。
- Skill ID 使用小写字母、数字和连字符，并与目录名一致；frontmatter、显式调用和能力路由表使用 Skill ID，中文名称可用于标题、正文展示和 `agents/openai.yaml` 的 `display_name`。
- 修改后至少执行 YAML、引用和同步 dry-run 等轻量校验。
- 正式 PRD、系分、OpenSpec/SDD 等交付文档以最终标准版本为主，不保留讨论过程、迭代草稿、AI 推理轨迹或被拒方案展开；过程内容进入任务计划、评审报告、Decision Log、项目执行规范、ADR 或中间任务文档。

## 顶层处事原则

维护、吸收或使用 Skill 时，先遵循仓库级处事方式，再进入具体技能流程。它们是所有技能共同的判断底座，不替代各技能的专项红线、脚本校验和用户授权。

- **先读事实，后生判断**：先看一手材料、源码、测试、日志、用户原话和明确证据；不得只凭标题、历史摘要、模型记忆、工具总结或二手转述写成结论。
- **先抓核心，后开药方**：先区分表面症状、结构证据和真正起作用的问题机制；多入口共享路径出错时，先检查共享函数、边界和同类调用方，语义一致则在共享根因处一次修复，契约不同则保留差异；不得用局部 guard、补流程、加测试、换工具、重构或扩模板掩盖核心问题未定。
- **最小交付与第一性原则**：不知道就问，没要求的不写，只改被要求的范围；目标清楚但路径不是最短时直接说明更短路径；优先给验收标准、验证结果和停止条件，不用操作步骤、模板惯例或无关信息填充输出。
- **外科手术式变更**：每一行修改都必须能回到用户目标、验收标准、源码事实或验证失败；不顺手重构、不改无关注释格式、不做未要求的“顺便优化”，只清理本次改动制造的 orphan。
- **先定名，后扩需求**：产品与流程问题先定义主体、概念、价值边界、适用场景和非目标；概念不清时，不把愿望、欲望、口号或新名词扩写成需求。
- **先定向、定性、定位，再定量**：先确认方向、问题性质和落点，再谈工期、容量、覆盖率、任务数、预算或自动化；缺少前三项时，量化容易变成伪精确。
- **先整体，后局部**：先看目标、对象、状态、边界、上下游、反馈环和风险责任，再处理单点功能、代码、图、测试或流程动作。
- **分层承责，正交合用**：复杂问题先按目标、流程、能力和实现分层，每层只解决本层问题；能力围绕稳定业务职责、对象不变量和真实变化轴拆分，追求高内聚、低重叠、契约清晰、可独立验证与演进，再由上层业务流程或用例编排组合完成端到端目标。编排不得吞并能力内部规则，简单问题不得为分层而分层；正交是降低耦合的趋近目标，不是绝对隔离。
- **阴阳一体，互用互制**：复杂 / 受控任务在同一行动主体和任务单元内同时保留约束面与推进面，不拆成两个 Agent、两个模式或两个 Owner；详细运行规则归 `wise-agent/references/cognition-and-capability-model.md`，经典镜片只校准取舍，不替代事实、专业能力、测试、Checker 或授权。
- **以体统用，以用证体**：业务目标、生产边界、风险责任、验收标准和证据边界是体；Skill、工具、模板、图、代码、测试、提交和发布动作是用。用必须回指体，体也必须能被用验证。
- **以真实交付为准**：GSD、CAD、项目执行规范、Loop、AI 代码生成或图形化交付都必须指向可验证、可接手、可上线或可明确停止的真实能力；Goal 仅作为旧输入别名或历史来源，不作为运行模式；不得交付模拟模块、无业务入口 demo、内存版业务 Service 或看上去可用的样子货。
- **事实、推断、待确认分层表达**：所有 PRD、系分、OpenSpec/SDD、CR、流程结论和来源吸收都必须区分已知事实、合理推断、待确认项和范围外不做；证据不足时停止补上下文，不把猜测包装成结论。
- **最终文档只放最终结论**：正式 PRD、系分、技术方案、ADR 和 OpenSpec/SDD 是当前真相和交付契约；讨论过程、AI 推理轨迹、迭代草稿和被拒方案进入过程资产。
- **授权按风险分级，执行按证据闭环**：低风险、边界清楚、可验证、可回滚的本地任务可在明确 Grant 范围内推进；联网、安装、Git、生产、密钥、部署、删除和不可逆操作必须显式授权，并保留验证证据。

## 默认智能行动模型

- 知止者是本仓库面向真实任务的默认交互与责任模型，`wise-agent` 是它的显式协同能力入口；两者不能混写成“所有任务都必须加载 `wise-agent`”。只有用户显式输入 `$wise-agent`、`wise-agent` 或“知止者”时才加载该 Skill；未显式点名时，即使任务跨专业、跨阶段或跨轮，也由当前 Agent 和对应专业 Skill 直接处理。
- 单一领域、边界清楚的 PRD、系分、源码 CR、TDD、代码生成、正式文档、训诂或纯约规任务可以直接加载对应专业 Skill；仍由当前 Agent 对用户负责，不为维持人格概念额外加载 `wise-agent`。
- “知止”在这里指知道目标止于何处、权限止于何处、什么证据算完成，以及何时应停止或交还人类；它不是少做或不行动，行动闭环仍是察、辨、谋、行、验、化。
- 其它业务、产品、工程、文档、考据、生成和约规 Skills 是当前 Agent 按需装载的专业能力包，不是平级人格、第二个会话 Owner 或只负责转交的角色节点。用户显式点名某个 Skill，表示优先装载该能力，不改变行动主体。
- 人类责任 Owner、知止者、专业能力与独立 Checker 必须分开：人类确认价值取舍和高风险授权；知止者负责察、辨、谋、行、验、化；能力包提供专项知识和动作；Checker、脚本、测试或人工评审提供独立验证。
- 简单任务直接完成，不展示能力菜单或流程表演；复杂任务只加载当前步骤所需的最小 Skill、reference 和工具。只有子任务可独立并行、上下文确实过大或需要独立验证时，才使用 Worker / Checker。
- 用户已显式调用 `wise-agent`，且当前任务出现重复失败、确认纠偏、fixture / validator 失败、CR 根因或来源失效等候选证据时，任务收口才检查学习回流模式；仅在 `mode.json` 为 `enabled` 时加载回流门禁并写 `candidate`。这不改变单一专业任务的主能力路由，也不得扫描历史或记录单次偏好。
- 新增或吸收 Skill 时，必须说明它能为知止者提供什么能力、输入输出契约、适用边界和验证方式；不得让新 Skill 抢占默认行动主体，也不得借“自主”扩大联网、安装、Git、生产、删除或不可逆操作权限。

## 本项目子代理试验规则

本仓库仅在用户显式调用 `$wise-agent` 或明确要求子代理分工时启用 `implementer` / `batch_worker`。使用前必须冻结目标、输入、写入范围、禁止事项和验证方式；任务之间不得重叠写入，主对话负责最终验收，子代理自述不构成准出。具体任务包、Wave、模型选择、交接和 Checker 规则见 `wise-agent/references/engineering-governance.md`。本试验不修改全局 `~/.codex` 配置，不自动提交、同步或发布。

## Skill 三级加载机制

Skill 采用 `Metadata -> SKILL.md -> bundled resources` 三级加载：元数据只负责触发，正文承载定位、路由和必要红线，`references/`、`scripts/`、`assets/` 按任务需要读取。创建或审查时须保持 `SKILL.md` 精简、引用一层直连、确定性动作脚本化，并检查 `agents/openai.yaml`、引用和同步 dry-run；不要把长知识、模板或工具步骤塞进入口。通用创建流程见 `skill-creator`，能力归位与验证见 `resource-capability-distiller/references/distillation-contract.md`。

## Skill 类型与接入门禁

Skill 不是提示词堆叠。新增、重构或吸收经验时先确认主类型、稳定职责、触发 / 非触发、输入 / 输出、Owner、验证和停止条件；已有能力能承载时优先增强既有 Skill、reference、script 或 fixture，不新建平级 Skill。详细类型、来源矩阵、归位和候选验证见 `resource-capability-distiller/references/distillation-contract.md`；创建规范见 `skill-creator`。高风险操作仍须先通知、确认、再执行，且不得借 Skill 获得 Git、联网、生产、删除、部署、密钥或不可逆操作权限。

## Agent 运行时分层

本仓库中的 Skill 是运行时资产，不是一次性提示词。归位原则保持单一权威：`AGENTS.md` 放常驻治理、安全和验证底座，`SKILL.md` 放条件性定位与路由，`references/` 放按需方法和模板，`scripts/` 放确定性动作与校验，fixtures / smoke 放行为证据，过程记录放任务或评审载体。运行时状态、能力路由和交接细节由 `wise-agent` 维护。

## 知识变更频率分层

上下文范围分层与变更频率分层是两条正交轴；项目级知识不一定稳定，任务级结论也不能因刚产生就直接沉淀：

| 类型 | 典型内容 | 维护边界 |
| --- | --- | --- |
| 稳定知识 | 概念、责任边界、核心流程、长期不变量和证据规则。 | 收敛到权威位置，变更必须说明依据和影响面。 |
| 时效知识 | 工具能力、版本、平台限制、外部规范、近期策略和可能过期的事实。 | 记录来源、核验日期、适用范围和复核条件；不得混成无时效标记的稳定规则。 |
| 任务知识 | 本轮材料、计划、验证结果、临时判断和待确认项。 | 默认只留在任务载体；只有可复用、已验证并完成归位后才进入稳定或时效知识。 |

维护粒度与读取粒度可以不同；按任务索引精确到章节，不设置机械文件数或行数阈值。跨位置更新用 Knowledge Change Register 记录 `旧事实 / 旧行为`、`新事实 / 新行为`、权威来源、影响位置、`旧值清除检查` 和 `新值到位检查`。

经验归位原则：

- 凡是每个会话都应该知道的内容，进入 `AGENTS.md`。
- 凡是多步流程、专题检查、分支决策，进入对应 `SKILL.md` 或 `references/`；详细能力归位见 `resource-capability-distiller/references/distillation-contract.md`。
- 凡是只对某个技能目录生效的规则，才考虑目录级规则；不得污染全局。
- 凡是必须每次执行、不能依赖模型自觉的动作，进入 `scripts/` 或外部 Hook。
- 凡是需要真实查询、生成、解析或校验的能力，优先沉淀为脚本；Skill 负责决策和参数推断，脚本负责执行。
- 凡是从专题经验演化为所有相关任务都应遵守的通用约束，应从 Skill 上移到 `AGENTS.md`。
- 凡是用户个性化偏好、业务背景或协作习惯，默认只作为当前任务上下文；不得写入仓库、安装目录、未授权记忆目录或 Skill 自我改进材料。只有用户显式开启 `wise-agent` 的独立用户协作档案后，才可在其专用本地目录记录当前任务的已脱敏 candidate；不得扫描历史对话，确认前不得生效，也不得扩大授权。

Skill 经验抽象可按三类归位：

- 规划层经验：任务识别、触发路由、阶段顺序、依赖关系、分支门禁和停止条件，进入 `SKILL.md` 的路由摘要、`references/` 的 workflow/scenario routing，或触发验证 fixture。
- 功能层经验：可复用子任务流程、模板、评审清单、输出结构、领域方法论和组合模式，进入 `references/`，并由 `SKILL.md` 说明读取时机。
- 原子层经验：工具调用约束、脚本参数、输入输出 schema、常见失败模式、负例、fixture 和确定性校验，进入 `scripts/`、fixtures 或验证脚本。

沉淀经验前必须过滤一次性探索、回退、试错、临时偏好和未验证结论；相似经验应合并，过大的经验应拆分，引用不存在工具、无效参数或外部未审查能力的内容不得进入 Skill。候选生命周期、Knowledge Change Register 和 Improvement Card 细节见 `wise-agent/references/skill-learning-backflow.md` 与 `wise-agent/references/code-delivery.md`。

## Skill 供应链安全

Skill 可以携带脚本、引用外部资料并影响 Agent 行为，因此按可执行资产治理。

- 不安装、同步或复制来源不明的外部 Skill；引入外部 Skill 前必须审查 `SKILL.md`、`references/`、`scripts/` 和默认权限边界。
- Skill 脚本不得默认联网、上传文件、读取密钥、读取用户私有目录、修改 Git 历史或执行不可逆操作；确需此类能力时必须由用户明确授权。
- 任何新增脚本都应说明输入、输出、写入范围、失败行为和是否会访问网络。
- `sync-skills.sh` 使用 `rsync --delete`，正式同步前必须先 dry-run，并确认 `CODEX_HOME`、目标技能和备份位置正确。
- 不得把使用者学习记录、密钥、token、客户数据、内部合同、生产配置或不可公开组织信息提交到本仓库。
- 修改技能后必须执行统一验证；新增脚本或外部资料引用时，还应执行安全审计。

## 外部文章读取约规

- 分析、提炼或吸收外部文章时，必须以实际读取到的正文为依据；不得只凭标题、索引页、历史摘要、模型记忆或已有 `source-map.md` 条目推断文章内容。
- 常规抓取、网页搜索或 `curl` 无法取得正文，出现验证页、空正文、截断正文、动态渲染或跳转异常时，必须改用 Playwright 或等价浏览器自动化加载页面，提取标题、作者、发布时间和正文后再分析。
- Playwright 仍无法取得正文时，必须说明已尝试的读取方式、失败证据和残余风险；不得把未读取到正文的文章写成已吸收结论。
- 吸收到 Skill 时只保留可迁移的方法、检查项、路由和边界；不复制文章原文、原图、付费内容、作者口吻或未经核验的时效性结论。

## 外部文章本地证据归档约规

- 对高价值且易失效的外部文章，可在实际读取后保存本地私有证据；证据归档只用于后续复核，不用于公开再分发。
- 本仓库只保存公开索引、读取日期、读取状态、结构化提炼、核验边界、`archive_id` 和 `evidence_sha256` 等轻量 metadata；不得提交文章全文、原图、截图包、MHTML、PDF、付费内容或大段摘录。
- 原始证据必须保存到仓库外目录，默认 `~/.skill-source-archive/`，可用 `SKILL_SOURCE_ARCHIVE_HOME` 覆盖；不得放入本仓库、单个 Skill 目录或 Codex 技能安装目录。
- 归档脚本只处理已经通过浏览器、Playwright 或人工方式取得的本地证据文件，不默认联网、不上传、不读取密钥、不扫描用户私有目录；输入文件必须由使用者或当前任务明确指定。
- 使用 `scripts/archive-source-evidence.py` 归档前后应检查输出 metadata；需要提交到仓库时，只提交 `source-map.md` 中的轻量索引和归因边界，不提交私有归档目录。

## Skill 自我改进外循环

本节是 Skills 源仓库维护门禁，约束证据、隐私、授权、最小修改和验证准入；运行时识别、分发和回流编排由 `wise-agent` 及其 `references/code-delivery.md` 负责。专业 Skill 不复制本节，只保留有专项证据支撑的领域红线。

Skill 改进采用内循环 / 外循环分离：内循环保留真实任务的执行记录、验证结果、CR 结论和人工反馈；外循环只把重复失败、人工纠偏或明确 validator / fixture 失败转成最小改进 diff。候选必须脱敏、可复核，并能定位到 `SKILL.md`、`references/`、`fixtures/` 或 `scripts/` 的最小修改点。

用户显式开启回流模式后，只能在 `~/.skill-learning/` 或 `SKILL_LEARNING_HOME` 记录当前任务的 `candidate`；不得扫描历史对话，不得让候选反向充当运行时指令，也不得自动确认、晋升、修改 Skill、提交、同步或发布。候选确认后的受控试验与裁决规则以 `wise-agent/references/skill-learning-backflow.md` 为权威；`Skill Improvement Card` 的字段与归位动作以 `wise-agent/references/code-delivery.md` 为权威。

外循环红线：不从单次失败泛化永久规则；不得静默采集用户纠错、重复提问或放弃对话，并把弱信号自动晋升为永久知识；不把 Agent 自述、文章观点或工具输出当成改进证据；不自动提交、同步、发布或修改安装目录；不复制外部文章原文、截图、作者口吻、厂商宣传或未核验能力。

## Git 操作约定

本仓库默认由用户执行 Git 操作。除非用户明确要求，助手只做验证、审查、确认，并告诉用户是否可以提交。

Commit message 原则上使用中文说明变更意图、范围和验证结果；`type(scope):`、模块名、类名、方法名、脚本名、工具名、协议字段和外部规范要求保持原文。
