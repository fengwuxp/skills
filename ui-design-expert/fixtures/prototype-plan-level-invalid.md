# 未选定层级的原型计划

## 目标、层级与边界

验证问题、范围、非目标、假设和 Owner 已列出，但同时把 L0、L1、L2 都写成当前交付层级。

## 页面、状态与真实内容

入口、退出、重置、页面图、状态图、loading、empty、error、权限、弱网和真实内容已列出。这些内容分别对应任务前后和各类异常场景。

## 交互与失败恢复

交互表包含 source、trigger、condition、action、destination、feedback、failure、overlay、setReactionsAsync、保留用户输入和焦点。每一行都描述操作前提和用户收到的结果。

## 工程交接与可访问性

Components、Code Connect、Figma variables、Auto Layout、语义命名、annotations 和 dev resources 均已列出。

交接指定 exact node，并要求 get_design_context、get_metadata、get_screenshot；同时列出项目组件、tokens、路由、状态、数据模式和 senior-software-architect 协作边界。

响应式覆盖桌面与移动，并写明键盘、焦点、可访问名称、对比和 200% 缩放。

## 验证证据与停止条件

记录 Figma preview、成功、校验失败、网络失败、frame 链接、版本、任务走查、截图、不证明可点击、MCP 输出不是生产代码、停止条件和残余风险。这些材料对应同一轮评审并可以回溯到责任人。
