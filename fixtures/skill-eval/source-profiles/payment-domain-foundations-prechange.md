# Payment domain foundations baseline

This evaluation baseline is the repository-owned `payment-expert` capability immediately before the systematic payment-domain learning entry was added.

- Git commit: `82bb646e0f2a0f0f4fb5cf575d1a8735780aac8f`
- Load from that commit:
  - `payment-expert/SKILL.md`

The baseline has no `payment-domain-foundations.md` and no route to it. This file is evaluation evidence, not a second Skill authority. Reconstruct the baseline with `git show <commit>:<path>`; do not use the working-tree `SKILL.md` for the baseline condition.
