# 禁止行为与权限边界

## 强制禁止行为

1. 使用 `System.out.println` 打印日志。
2. 使用 `e.printStackTrace()` 输出异常。
3. 提交 `.env` 文件、密钥、token 到版本控制系统。
4. 在未告知用户的情况下执行外部 API 调用，例如发送邮件、创建 PR、提交 commit。
5. 修改 `.gitignore`、CI/CD、构建、部署等核心配置文件，除非任务明确要求。
6. 在修复 Bug 时顺手重构不相关的代码或文件。
7. 在代码中添加“预留接口”“未来扩展”等任务范围外的抽象。
8. 使用已经标记为 `@Deprecated` 的类或方法。
9. 使用 `Vector`、`Hashtable`、`Stack` 等过时集合类。
10. 使用 `new BigDecimal(double)` 构造金额。
11. 在 `foreach` 循环中直接对集合执行 `remove` / `add`。
12. 返回 `null` 代替空集合或 `Optional`，除非接口契约明确允许。
13. 在事务方法中捕获异常但不重新抛出或显式回滚。
14. 在没有明确边界、数据归属、运维能力和故障隔离方案时拆分微服务。
15. 让核心业务逻辑直接依赖 Controller、Mapper、ORM Entity、第三方 SDK、MQ、缓存或 HTTP Client。
16. 破坏对外 API、DTO、枚举、错误码、消息体、配置项或数据库字段兼容性而不提供迁移方案。
17. 将密码、token、密钥、身份证、银行卡、手机号等敏感信息原文写入日志、测试数据或异常消息。
18. 对生产数据执行删除、批量更新、修数、重放消息、重建索引、清理缓存等操作时没有 dry-run、备份、回滚和影响范围说明。
19. 在涉及权限、租户、资金、审批、配置或敏感数据的场景缺失审计日志。
20. 用“最终一致”掩盖没有重试、补偿、对账、告警和人工兜底的问题。
21. 为尚未确认的未来需求添加通用平台、规则引擎、插件体系、多租户、分库分表、中台或复杂扩展点。
22. 新增第三方依赖、starter、中间件或代码生成器而不说明必要性、替代方案、版本风险和维护责任。

## 权限边界

| 分类 | 权限 | 操作 |
|------|------|------|
| 直接执行 | 无需额外确认 | 读取文件、搜索代码、生成方案、运行安全的本地编译/测试/静态检查 |
| 需要确认 | 需用户确认或遵守当前工具权限 | 修改文件、创建文件、执行耗时命令、调用第三方 API、提交 commit、创建 PR |
| 禁止执行 | 绝不主动执行 | 发送邮件/消息、push 到远程、部署生产环境、删除数据、修改 CI/CD 配置、无回滚方案的生产修数 |

## 常见违规示例

- 错误：在 Service 中直接使用 `LambdaQueryWrapper`。应改用 `XxxRefs` 字段常量类或项目统一查询 helper。
- 错误：Controller 返回实体对象 `User`。应返回 `UserDTO` / `UserVO`。
- 错误：`queryXxxById` 返回 `null`。应返回 `Optional`、可空标注对象，或抛出业务异常。
- 错误：使用 `new Date()`。应使用 `LocalDateTime.now()` 或注入时钟以便测试。
- 错误：事务方法中 `catch` 异常后不抛出。应重新抛出或显式设置回滚。
- 错误：Controller 中直接写库存扣减、余额变更、权限判断等业务规则。应下沉到应用服务/领域服务并用测试保护。
- 错误：新增外部 SDK 后在业务服务里到处直接调用。应通过 `XxxClient` / `XxxGateway` / `XxxOperations` 端口隔离。
- 错误：线上修数 SQL 没有 dry-run、备份、回滚脚本和影响行数确认。应先出修数方案并经确认后执行。
- 错误：为了一个不确定的后续需求提前引入规则引擎或插件体系。应先用明确代码完成当前需求。

## 规约冲突处理

如果项目现有代码与规范不一致：

1. 先判断本次任务是否必须触碰该问题。
2. 新增代码和局部修改按当前规范执行。
3. 旧代码大范围统一需要单独评估、拆分任务和征得用户确认。
4. 禁止借修复 Bug 之名擅自大规模重构旧代码。
