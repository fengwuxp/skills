# Java 编码规约 2.0

本文档用于约束 Java/Spring 项目的编码行为，定位为团队 Java 编码治理标准。它以阿里巴巴 Java 开发手册 / P3C 的分级、分类和工程实践为基础，结合团队 Java/Spring/Wind 项目约规、现代工具链和架构边界要求形成。模块划分、服务分层、CRUD 命名、MyBatis Flex 详细实践、数据库设计、日志体系、测试体系和 Git 流程，以 `project-governance-standards.md`、`workflow.md` 为准；编码 Review 的判断顺序和问题定级见 `coding-review-deep-dive.md`；禁止项清单以 `negative-constraints.md` 为准。

## 目录

- `1. 编码规约总纲`
- `2. 规约等级定义`
- `3. 阿里 Java 规约采纳原则`
- `4. 编程规约`
- `5. 契约规约`
- `6. 异常与日志规约`
- `7. Java/Spring 专项规约`
- `8. Lombok 与 MapStruct 规约`
- `9. 不可变对象规约`
- `10. 时间、金额、ID 规约`
- `11. 数据库与 MySQL 规约`
- `12. MyBatis / MyBatis Flex 规约`
- `13. 分层与模型规约`
- `14. 安全编码规约`
- `15. 测试代码规约`
- `16. Review 定级与整改建议`
- `17. 自动化检查映射`
- `18. 阿里规约采纳矩阵`
- `19. 与其他规范的关系`

## 1. 编码规约总纲

- **可读优先**：代码首先写给人看，命名、结构、注释和测试应能表达业务意图。
- **边界清晰**：Controller、Application、Domain、Infrastructure、Converter、Repository、Test 各司其职，不穿透职责边界。
- **契约明确**：入参、返回、异常、空值、权限、幂等、分页、兼容和序列化语义必须清楚。
- **失败可控**：异常、事务、重试、补偿、日志、告警和人工兜底必须匹配业务风险。
- **一致可治**：团队约规优先于个人偏好；局部风格服从项目已有规范和自动化检查。
- **验证闭环**：重要规则必须有测试、静态检查、Review 清单或运行指标支撑。

## 2. 规约等级定义

- **【强制】**：必须遵守。违反后可能导致错误、事故、安全风险、兼容破坏或团队一致性失控。
- **【推荐】**：默认采用。确需偏离时，应说明原因、影响范围和替代保护。
- **【参考】**：可根据业务、技术栈、历史包袱和项目阶段选择性采用。

## 3. 阿里 Java 规约采纳原则

- 阿里巴巴 Java 开发手册 / P3C 作为团队 Java 基础规约底座，覆盖编程规约、异常日志、MySQL、工程规约和安全规约。
- 团队直接采纳成熟基础项，例如命名、常量、集合、异常、日志、MySQL 基础规则和安全红线。
- 团队增强现代实践，例如 Java 17+/21、Spring Boot 3+、JSpecify、Jakarta Validation、Lombok、MapStruct、不可变对象、序列化兼容、MyBatis Flex、Wind 项目族。
- 项目已有明确规范且与阿里规约不完全一致时，先遵循项目规范；新代码和局部修改应向当前团队约规收敛。
- 不机械照搬。阿里规约没有覆盖或不适配当前技术栈的内容，按团队架构边界、生产风险和可维护性要求补充或调整。

## 4. 编程规约

### 4.1 命名规范

- 【强制】代码命名不得以下划线或美元符号开始或结束。
- 【强制】严禁拼音与英文混用，不允许直接使用中文命名。
- 【强制】类名使用 `UpperCamelCase`，但 `DO`、`BO`、`DTO`、`VO`、`AO`、`PO`、`UID` 等通用缩写除外。
- 【强制】方法名、参数名、成员变量、局部变量使用 `lowerCamelCase`。
- 【强制】常量命名使用 `UPPER_UNDERSCORE`，例如 `MAX_STOCK_COUNT`。
- 【强制】抽象类使用 `Abstract` 或 `Base` 开头；异常类使用 `Exception` 结尾；测试类以被测类名开头、`Tests` 结尾。
- 【强制】POJO 布尔类型变量不要加 `is` 前缀，避免序列化和框架识别歧义。
- 【强制】同一业务概念必须使用同一术语，禁止同义词混用，例如 `account`、`wallet`、`balance` 未定义前不得混用。
- 【推荐】如果模块、接口、类或方法使用设计模式，命名时体现模式语义，例如 `XxxStrategy`、`XxxFactory`、`XxxAdapter`。
- 【推荐】避免使用过宽词汇掩盖职责，例如 `handle`、`process`、`manager`、`data`、`info`、`logic`。

### 4.2 常量与枚举

- 【强制】不允许魔法值直接出现在代码中。
- 【强制】`long` 或 `Long` 初始赋值使用大写 `L`，不得使用小写 `l`。
- 【推荐】不要使用一个大而全的常量类维护所有常量，应按功能和作用域归类。
- 【推荐】常量按复用层次放置：跨应用共享、应用内共享、子工程内共享、包内共享、类内私有。
- 【推荐】取值范围固定且有业务含义时，优先使用枚举。
- 【强制】业务枚举类默认统一放到当前模块或业务包的 `enums` 包，例如 `com.xx.xx.enums`；确需放在更内聚的子域包时，必须保持包职责清晰并说明原因。
- 【强制】业务枚举必须实现 `DescriptiveEnum`；如果项目已有等价描述接口，必须说明兼容关系，禁止只靠枚举名承载展示语义。

### 4.3 OOP 与对象设计

- 【强制】所有覆写方法必须加 `@Override` 注解。
- 【强制】禁止使用过时的类或方法；外部接口兼容场景除外，但必须说明原因。
- 【强制】禁止使用 `Vector`、`Hashtable`、`Stack` 等过时集合类。
- 【强制】外部正在调用或二方库依赖的接口，不允许随意修改方法签名。
- 【推荐】优先组合而非继承；继承必须表达稳定的“是一个”关系。
- 【推荐】接口表达能力边界，实现类承载技术细节，不要把实现约束泄露到调用方。
- 【推荐】对象暴露有语义的行为，不暴露任意 setter 让外部拼业务规则。

### 4.4 集合处理

- 【强制】判断集合是否为空，使用 `isEmpty()`，不要使用 `size() == 0`。
- 【强制】`Arrays.asList()` 返回的列表不能执行 `add`、`remove`、`clear`。
- 【强制】不要在 `foreach` 循环中直接 `add` 或 `remove` 元素，应使用 `Iterator` 或收集后统一处理。
- 【强制】`subList` 结果不可强转为 `ArrayList`，且要注意它与原列表的视图关系。
- 【推荐】对外返回集合时返回空集合，不返回 `null`。
- 【推荐】集合入参需要明确是否允许空集合、重复元素和顺序语义。
- 【推荐】不可变对象持有集合、数组、`Date` 等可变成员时，构造时做防御性拷贝，返回时给不可变视图、副本或更安全的值类型。

### 4.5 并发处理

- 【强制】线程资源必须通过线程池提供，不允许在业务代码中随意显式创建线程。
- 【强制】单例对象、缓存对象、共享状态必须保证线程安全。
- 【强制】禁止将线程不安全对象定义为静态共享实例，例如未加保护的 `SimpleDateFormat`。
- 【强制】异步任务必须处理异常，不能让异常静默丢失。
- 【推荐】时间格式化优先使用 `DateTimeFormatter`。
- 【推荐】高并发场景优先减少共享状态，锁区块只覆盖必要代码。
- 【推荐】异步任务必须明确线程池、超时、取消、异常处理、上下文传递和关闭策略。
- 【推荐】涉及重复请求、重复消息、乱序回调和并发提交时，必须设计幂等和并发测试。

### 4.6 控制语句与代码格式

- 【强制】`if`、`for`、`while`、`switch`、`do` 等保留字与括号之间必须有空格。
- 【强制】二目、三目运算符左右两边必须有空格。
- 【强制】注释的双斜线与注释内容之间保留一个空格。
- 【强制】单行字符数不超过 120 个；确因链式调用或测试数据需要超出时，应保证可读性。
- 【强制】普通类型引用优先使用 `import`，不要在字段、方法参数、返回值、泛型和局部变量中随意写全限定类名。
- 【推荐】控制语句避免深层嵌套，优先使用卫语句、提取方法或策略对象表达分支。
- 【推荐】项目格式化规则以 `.editorconfig`、IDEA Code Style、Checkstyle 或项目现有配置为准。

### 4.7 复用与方法抽取

- 【强制】不得单纯为了“看起来复用”而提取公有方法或工具类；提取前必须确认复用的是稳定业务语义、算法规则、协议转换或基础能力，而不只是重复的几行代码形状。
- 【强制】公有方法参数不得超过 5 个；编码或重构时如果确实需要超过 5 个参数，必须先和用户确认原因、调用方数量、兼容影响和替代方案。
- 【强制】不得为了隐藏复杂度而制造过深调用链；抽取方法后仍应能从调用点理解业务主流程、状态变化和副作用。
- 【推荐】DTO、VO、Request、Response、Query、Command 等模型的构建逻辑通常没有跨业务复用价值；除非存在稳定映射规则、兼容转换或多处强一致契约，否则不要提取为公有工具方法。
- 【推荐】避免一行代码方法复用；除非该方法承载明确领域语言、权限边界、异常语义、埋点审计或可替换策略，大多数一行包装只会增加跳转成本。
- 【推荐】代码复用应同时评估复用频率、业务语义稳定性、调用方一致性、可读性、测试边界和未来变化方向；可读性和逻辑内聚优先于机械去重。
- 【推荐】超过 3 个参数的公有方法优先评估参数对象、Command、Query、Options 或 Builder；超过 5 个参数按强制规则处理。

### 4.8 注释与 Javadoc

- 【强制】公共接口、公有方法、DTO/Request/Query、配置属性、扩展点必须有 Javadoc。
- 【强制】接口必须在类型 Javadoc 中说明职责、适用边界、调用方、线程/事务/权限/幂等等关键约束；公有方法必须完整说明业务语义、参数、返回值、异常、空值语义和副作用。
- 【推荐】注释、Javadoc、测试方法说明和代码内业务解释默认使用中文，优先说明业务语义、边界、风险和示例；协议字段、标准术语、框架 API、类名、方法名、错误码和外部系统名称保持原文。
- 【强制】注释说明“为什么这样做、边界是什么、风险是什么”，不重复代码显而易见的行为。
- 【强制】禁止保留大段注释掉的废弃代码；但不得擅自修改或删除有明确用途的注释代码，例如兼容性说明、历史协议样例、待启用配置、临时降级开关说明、测试夹具说明、迁移参考或用户明确要求保留的代码片段。确需清理时，必须先询问用户并获得确认。
- 【推荐】复杂规则应给出业务语义、输入输出、边界条件和示例。
- 【推荐】临时方案应标注原因、影响范围、替代方案或计划清理时间。

## 5. 契约规约

- 【强制】公共 API 的空值、异常、权限、幂等、分页、默认值和兼容语义必须明确。
- 【强制】空值处理按边界分层：内部 Java 契约使用 `org.jspecify.annotations` 相关注解；会暴露到 API 层的数据模型使用 Bean Validation；其他业务前置条件、状态条件和不可达分支优先使用 `AssertUtils`。
- 【强制】公共 API 的空值语义必须明确，必要时使用 `javax.validation` 的 `@NotNull`、`@NotBlank`、`@NotEmpty` 等验证注解；项目已迁移 Jakarta EE / Spring Boot 3+ 时，可使用 `jakarta.validation` 等价注解，但不得在同一模块内混用两套 validation 包。
- 【强制】内部 Java 契约优先使用 `org.jspecify.annotations.Nullable`、`NonNull`、`NullMarked` 等注解表达可空与非空语义。
- 【强制】已由 JSpecify 标注为非空的参数、返回值和字段，不再额外添加无业务语义的 `AssertUtils.notNull` 或同类空判断；只有业务前置条件、状态条件、集合内容约束、查不到数据、不可达分支等运行时业务事实才使用断言。
- 【强制】不要把 `Optional` 用作字段、DTO 属性或序列化模型属性。
- 【强制】不得用 Bean Validation 替代所有内部断言；Validation 面向 API 契约和数据模型，`AssertUtils` 面向业务前置条件、状态校验和内部防御式编程。
- 【推荐】可能不存在的查询结果使用 `findXxx` 或 `Optional<T>` 表达。
- 【推荐】必然存在的查询使用 `getXxx` 表达；查不到时抛业务异常。
- 【推荐】普通字段、参数、返回值使用 import 后的注解；只有 type-use 位置存在歧义时，才使用全限定注解。

## 6. 异常与日志规约

### 6.1 异常处理

- 【强制】不要捕获 `NullPointerException`、`IndexOutOfBoundsException` 等运行时异常来替代代码检查。
- 【强制】禁止吞异常；捕获异常后必须处理、包装、补充上下文或重新抛出。
- 【强制】事务方法中捕获异常后，必须重新抛出或显式回滚。
- 【强制】禁止直接使用 `e.printStackTrace()`。
- 【推荐】断言优先于手动抛异常：API 数据模型约束使用 Bean Validation；业务前置条件、状态校验和内部防御式编程使用项目统一断言工具，例如 `AssertUtils`。
- 【推荐】手动抛异常只用于断言工具无法清晰表达的场景，例如需要组合复杂上下文、包装第三方异常或构造特定错误码。
- 【推荐】业务异常继承或使用项目统一的 `BaseException`。
- 【推荐】包装第三方异常时保留 cause，避免丢失原始诊断信息。
- 【推荐】用户提示和内部诊断信息分层处理。

### 6.2 日志基础规则

- 【强制】统一使用 SLF4J 门面，不使用 `System.out.println` 打印日志。
- 【强制】日志使用占位符，不使用字符串拼接。
- 【强制】错误日志必须包含错误消息和异常堆栈，异常对象作为最后一个参数传入。
- 【强制】日志不得输出密码、token、密钥、身份证、银行卡、手机号等敏感信息；必要时脱敏。
- 【推荐】日志变量名统一使用 `logger` 或 `LOG`。
- 【推荐】日志内容采用 key/value 风格，保留 traceId、业务 ID、用户 ID、租户、请求来源、外部调用目标等关键上下文。
- 【推荐】不重复打印同一个异常；异常如果会被全局处理器打印，业务层只在必要时补充上下文。

```java
logger.info("Processing trade, tradeId = {}, symbol = {}", tradeId, symbol);
logger.error("Handle payment error, orderNo = {}, message = {}", orderNo, exception.getMessage(), exception);
```

## 7. Java/Spring 专项规约

- 【强制】禁止字段注入；Spring Bean 优先使用构造注入。
- 【推荐】Spring Bean 构造注入优先使用 `final` 依赖字段 + Lombok `@AllArgsConstructor`。
- 【推荐】若 Bean 中存在非依赖状态、可选依赖或特殊构造逻辑，应改用显式构造器并说明原因。
- 【强制】事务边界必须在应用服务或明确的用例边界上表达；不得在 Controller 中承载事务业务规则。
- 【强制】事务内避免不可控远程调用、长耗时计算和无上限循环；确需调用时必须说明超时、补偿和失败处理。
- 【推荐】Configuration Properties 必须有清晰前缀、字段说明、默认值和校验。
- 【推荐】核心业务链路应接入日志、指标、trace 和 Actuator/Observability 等项目统一观测能力。

## 8. Lombok 与 MapStruct 规约

### 8.1 Lombok

- 【强制】Lombok 只用于减少样板代码，不得隐藏业务不变量、状态变更、副作用和领域行为。
- 【强制】领域对象、有业务行为的类、继承层次复杂的类，不得无脑使用 `@Data`、`@Setter`、`@AllArgsConstructor`。
- 【强制】`@ToString`、`@EqualsAndHashCode` 必须避开敏感字段、大字段、双向关联和懒加载对象，避免信息泄露、递归和性能问题。
- 【推荐】简单 DTO、VO、Query、Command、Event 可使用 `@Getter`、`@Setter`、`@NoArgsConstructor`、`@Builder`，但公共契约语义必须清晰。
- 【推荐】Spring Bean 构造注入优先使用 `final` 依赖字段 + `@AllArgsConstructor`；特殊构造逻辑使用显式构造器。

### 8.2 MapStruct

- 【强制】MapStruct 只负责模型转换，不承载业务规则、外部调用、数据库查询、权限判断、状态流转或审计写入。
- 【强制】跨层模型转换优先集中在 Converter/MapStruct 层，禁止在业务代码中散落 `BeanUtils`、反射拷贝或手写重复转换。
- 【强制】Converter 放在 `mapstruct` 包或项目既有等价转换包中，命名为 `XxxConverter`；Wind/Nobe 风格项目优先使用 `services.mapstruct` 包。
- 【强制】MapStruct 方法命名使用 `convertToXxx`、`convertToXxxDTO`、`convertToEntity` 等能表达目标模型的名称，不使用 `to`、`build`、`copy` 这类语义过弱的方法名。
- 【强制】有语义差异、字段重命名、枚举转换、空值策略、默认值的映射必须显式声明，并补充测试覆盖。
- 【强制】更新已有对象时必须明确 null 处理策略，避免源对象的 null 意外覆盖目标对象。
- 【推荐】关键转换测试覆盖字段完整性、枚举、空值、默认值、嵌套对象和集合转换。

## 9. 不可变对象规约

- 【推荐】值对象、配置对象、规则参数、领域事件、审计快照、API 输出模型、缓存 Key 和跨线程传递对象优先设计为不可变对象，可使用 `record`、`final` 字段、无 setter、构造器校验、防御性拷贝和不可变集合表达。
- 【推荐】聚合根、状态机对象、ORM Entity、MyBatis DO、批量填充模型和框架反射/序列化强依赖可变结构的对象，不强行不可变；但状态变化必须通过有语义的方法、应用服务或状态机推进，不暴露任意 setter。
- 【推荐】不可变对象持有集合、数组、`Date` 等可变成员时，构造时做防御性拷贝，返回时给不可变视图、副本或更安全的值类型。
- 【推荐】不可变 DTO、事件、消息或快照若需要被 Jackson、Kryo、JDK Serialization、Protobuf 等序列化库处理，必须验证构造器、record、final 字段、默认值、字段别名、版本兼容和不可变集合是否能稳定序列化/反序列化；必要时显式声明 `@JsonCreator`、`@JsonProperty`、无参构造器、专用序列化器或转换层。

## 10. 时间、金额、ID 规约

- 【强制】金额使用 `BigDecimal`，禁止使用 `new BigDecimal(double)`。
- 【强制】金额字段必须明确币种、精度和舍入规则。
- 【强制】时间必须明确时区、格式和精度；跨系统传输时必须说明序列化格式。
- 【推荐】新代码优先使用 `LocalDateTime`、`LocalDate`、`LocalTime`，避免继续引入 `Date`。
- 【推荐】时间格式化优先使用 `DateTimeFormatter`。
- 【推荐】涉及当前时间、随机数、ID 生成的逻辑，应考虑可测试性和可替换性。
- 【推荐】ID 生成必须考虑唯一性、趋势性、可追踪、并发、分库分表和外部暴露风险。

## 11. 数据库与 MySQL 规约

- 【强制】表名、字段名使用小写字母、数字和下划线，禁止数字开头。
- 【强制】表必须包含 `id`、`gmt_create`、`gmt_modified`。
- 【强制】禁止数据库外键和级联删除，外键关系在应用层维护。
- 【强制】业务唯一约束必须使用数据库唯一键保证。
- 【强制】新增必填字段必须有默认值，否则字段应允许为空并配合兼容逻辑。
- 【强制】删除字段、改字段名、改字段类型必须先做兼容发布，再执行 DDL。
- 【推荐】可选通用字段包括 `creator`、`modifier`、`order_index`，以项目数据库规范为准。
- 【推荐】表达是与否概念的字段使用 `is_xxx` 命名，具体类型以项目数据库规范为准。
- 【推荐】高频查询条件必须评估索引；排序字段需要结合索引设计。
- 【推荐】深分页使用游标分页、搜索引擎或离线导出方案，不直接无限制 offset。

## 12. MyBatis / MyBatis Flex 规约

- 【强制】MyBatis Flex 项目禁止使用 `LambdaQueryWrapper`；查询条件必须使用 MyBatis Flex 生成的 `XxxRefs` 常量类，或项目统一的查询 helper 基于 `XxxRefs` 构造条件。
- 【强制】查询禁止随意使用裸字符串字段名；只有兼容历史 SQL、动态字段白名单或框架限制等特殊场景才可使用字符串字段名，并必须说明原因和保护措施。
- 【强制】QueryWrapper 构造逻辑应集中在 helper 或基础服务，避免到处手写条件。
- 【强制】需要将字段更新为 null 时必须显式指定更新列，并处理 `gmt_modified`。
- 【推荐】一般插入使用 `insertSelective`。
- 【推荐】一般更新默认使用 selective 方法，避免无意覆盖空值；确需将字段更新为 null 时，必须显式指定更新列、说明业务语义，并处理 `gmt_modified`。
- 【推荐】复杂 SQL 必须说明索引、分页、排序、数据量和慢查询风险。

## 13. 分层与模型规约

| 层次/模型 | 主要职责 | 红线 |
| --- | --- | --- |
| Controller / API | 参数校验、鉴权入口、调用用例、响应转换 | 不写领域规则，不直接访问 Repository/Mapper |
| Application / Use Case | 编排业务用例、事务边界、权限校验、调用领域能力和端口 | 不泄露框架细节，不承载复杂 SQL |
| Domain | 业务不变量、状态流转、领域规则、领域事件 | 不依赖 Web、ORM、MQ、Redis、HTTP Client、第三方 SDK |
| Infrastructure | Repository、Gateway、Client、MQ、缓存、文件、SDK 适配 | 不写业务决策，不吞外部异常 |
| Converter / Mapper | 模型转换、字段映射、枚举转换、默认值转换 | 不做权限判断、远程调用、数据库查询、状态流转、审计写入 |
| DTO / VO / Query / Command | 对外或跨层契约、字段约束、序列化语义 | 不承载复杂业务行为，不隐藏兼容语义 |
| Test | 表达业务行为、边界、异常、兼容和回归 | 不只为覆盖率造数据，不无断言 |

模型命名：

| 类型 | 命名 |
| --- | --- |
| 创建请求 | `CreateXxxRequest` |
| 更新请求 | `UpdateXxxRequest` |
| 不区分创建/更新 | `SaveXxxRequest` |
| 命令对象 | `XxxCommand` |
| 查询对象 | `XxxQuery` |
| DTO | `XxxDTO` / `XxxDetailDTO` / `XxxListDTO` |
| VO | `XxxVO` |
| 事件 | `XxxEvent` |
| 转换器 | `XxxConverter` |

- 【强制】Query 使用 `XxxQuery`，Request 使用 `XxxRequest`，DTO 使用 `XxxDTO`；创建、更新、保存等语义通过 `CreateXxxRequest`、`UpdateXxxRequest`、`SaveXxxRequest` 等前缀区分。
- 【强制】对外 API 和跨模块契约使用 DTO、Request、Query，不暴露 Entity、Mapper、Repository、内部状态机对象或持久化实现细节。
- 【强制】Controller / Web API / face/api 对外契约禁止直接接收或返回 Entity；ApplicationService、DomainQueryService 对外返回 DTO/VO/Result，不把 Entity 泄露到模块边界外。
- 【推荐】模块内部基础服务可以使用 Entity，但跨层、跨模块、跨系统传递时必须通过 Converter 转为 DTO、Request、Query、Command 或 Event。

## 14. 安全编码规约

- 【强制】公网 API 必须经过认证与授权。
- 【强制】权限模型应区分资源、角色、权限、用户/主体和数据归属。
- 【强制】日志和异常响应禁止泄露密钥、token、SQL、内部路径和敏感个人信息。
- 【强制】密钥、token、证书、生产地址和个人凭证不得进入代码库、镜像、测试数据或日志。
- 【强制】文件上传必须校验类型、大小、扩展名、内容嗅探、存储路径和访问权限。
- 【推荐】内网 secure API 应验签或使用项目统一服务间认证机制。
- 【推荐】Token 应考虑过期、刷新、撤销和设备隔离。
- 【推荐】验证码应考虑有效期、验证次数、发送次数和流控。
- 【推荐】MFA 或二次确认用于资金、权限、敏感信息变更等高风险动作。
- 【推荐】引入依赖必须关注漏洞扫描、许可证风险、维护责任和替代方案。

## 15. 测试代码规约

- 【强制】测试代码等同生产代码维护，命名、结构、测试数据、辅助方法和断言都必须清晰。
- 【强制】测试类以被测类名开头、`Tests` 结尾；测试方法以 `test` 开头，或遵循项目已有命名规范。
- 【强制】每个测试方法至少一个断言。
- 【强制】每个测试用例必须能表达测试场景、业务意图、输入条件、触发行为、输出结果和预期断言；复杂场景的说明写在测试方法 Javadoc 或方法级注释上，不写在方法体内部。
- 【推荐】一个测试只验证一个逻辑分支。
- 【推荐】异常和边界场景使用 `testXxxWithYyy` 命名。
- 【推荐】测试结构采用 Given/When/Then 或 Arrange/Act/Assert，避免准备、执行和断言混杂。
- 【推荐】Spring 上下文按需加载，不滥用全量启动。
- 【推荐】时间、随机数、外部接口、线程池、副作用通过接口注入，以便测试。
- 【推荐】新增缺陷修复必须补回归测试。

## 16. Review 定级与整改建议

Review 优先使用 `coding-review-deep-dive.md` 的判断顺序：业务语义、边界方向、契约完整性、失败路径、工程一致性。

| 等级 | 编码场景 |
| --- | --- |
| P0 | 资金、权限、租户、敏感数据、生产数据、事务一致性会被破坏；可能造成不可逆事故。 |
| P1 | 公共契约破坏、核心流程错误、严重 NPE、事务吞异常、越权风险、重复扣减或状态错乱。 |
| P2 | 边界穿透、缺少幂等/测试/日志、MapStruct 隐式错映射、Lombok 暴露 setter、可维护性明显下降。 |
| P3 | 命名不准、注释欠清晰、局部重复、格式不一致、低风险工具方法可读性问题。 |

整改建议必须包含：

- 问题：具体代码位置和行为。
- 风险：真实业务、数据、稳定性、安全或维护后果。
- 建议：最小可落地修改，不扩大范围。
- 验证：需要补充的单测、集成测试、契约测试或手工验证。

## 17. 自动化检查映射

| 检查项 | 工具或方式 |
| --- | --- |
| 阿里基础规约 | P3C / PMD / IDE 插件 |
| 格式与导入 | Checkstyle / Spotless / IDE Code Style / `.editorconfig` |
| 缺陷与坏味道 | SpotBugs / PMD / SonarLint / SonarQube |
| 依赖与漏洞 | Maven/Gradle dependency check、SCA、镜像扫描 |
| 架构边界 | ArchUnit、模块依赖扫描、包依赖检查 |
| 测试质量 | JUnit 5、Mockito、AssertJ、Testcontainers、覆盖率门禁 |
| 契约兼容 | API 契约测试、Schema 测试、消息兼容测试 |

自动化检查不能替代 Review。工具擅长发现确定性问题，架构师必须判断业务语义、边界、契约、失败路径和生产风险。

## 18. 阿里规约采纳矩阵

| 阿里规约板块 | 团队采纳方式 | 团队增强点 |
| --- | --- | --- |
| 命名规范 | 增强采纳 | 增加领域语言统一、模型后缀、Wind/项目命名习惯。 |
| 常量定义 | 直接采纳 | 补充分层常量、业务枚举描述接口。 |
| OOP 规约 | 增强采纳 | 补充领域对象、不可变对象、聚合根、状态机边界。 |
| 集合处理 | 直接采纳 | 补充不可变集合、防御性拷贝、返回空集合。 |
| 并发处理 | 增强采纳 | 补充线程池、上下文传递、异步异常、幂等和并发测试。 |
| 控制语句 | 直接采纳 | 保持简洁，避免过度嵌套。 |
| 注释规约 | 增强采纳 | 测试注释放方法上，说明场景、输入、输出、预期。 |
| 异常规约 | 增强采纳 | 补充 `BaseException`、`AssertUtils`、事务回滚、cause 保留。 |
| 日志规约 | 增强采纳 | 补充 traceId、业务 key/value、敏感信息红线。 |
| MySQL 规约 | 增强采纳 | 补充团队字段、迁移、兼容、dry-run、回滚要求。 |
| 工程规约 | 增强采纳 | 补充模块边界、分层依赖、workflow、Git 默认由用户操作。 |
| 安全规约 | 增强采纳 | 补充租户隔离、审计、密钥、敏感数据、后台高危操作。 |
| 单元测试 | 增强采纳 | 补充测试资产治理、方法级注释、Given/When/Then。 |
| 代码生成/工具 | 团队增强 | 补充 Lombok、MapStruct、JSpecify、Bean Validation。 |

## 19. 与其他规范的关系

- `coding-review-deep-dive.md`：编码 Review 判断顺序、分层检查、问题定级和整改建议。
- `project-governance-standards.md`：模块划分、服务分层、CRUD 命名、API、数据库、日志、安全、测试、Git、前端、K8s 等项目细则。
- `clean-code.md`：Clean Code、Clean Architecture、命名、函数、注释、对象、错误处理、测试、系统演进和并发的审美与启发。
- `testing.md`：测试驱动设计、测试分层、测试代码整洁、测试资产治理和 Review 清单。
- `testing-practices.md`：Java/Spring/Wind 测试、Spring 测试减负、复杂业务测试手段、资金域断言和非 Java 技术栈适配。
- `workflow.md`：代码修改、验证命令、PR 和 Git 流程。
- `negative-constraints.md`：禁止行为和权限边界。
