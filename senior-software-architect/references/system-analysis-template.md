# 系统分析设计模板

本文承载可复制系统分析设计模板。主规则、写作要求、评审清单和触发边界见 `system-analysis-design.md`；只有当用户要求“写系分”“系统分析设计”“详细设计”“技术详细方案”或“按团队系分模板输出”时才读取本文。

## 使用时机

- 需要直接生成完整系统分析设计文档。
- 需要按团队系分模板补齐章节。
- 需要把需求转成可评审、可编码、可验收的设计文档骨架。

## 不适用场景

- 只做系统设计 CR、结构检查或判断是否需要系分时，先读 `system-analysis-design.md`。
- 只做架构图时，读 `diagram-output.md`。
- 只做生产上线检查时，读 `production-readiness.md`。

## 读取后必须产出

- 一份经过裁剪的系统分析设计文档；不适用章节必须写明“不适用原因”，不要直接删除高风险章节。

## 需要继续读取的 reference

- 系分写作要求和评审清单读 `system-analysis-design.md`。
- 图形化表达读 `diagram-output.md`。
- 生产风险读 `production-readiness.md`。
- 一致性读 `distributed-consistency.md`。
- 安全读 `security-architecture.md`。

## 按任务读取索引

| 任务 | 优先读取 | 跳过 |
| --- | --- | --- |
| 生成完整系分 | 1、2、3、4、5、6 | 专项规则长解释 |
| 只补背景和目标 | 1 | 详细设计模板 |
| 只补概要设计 | 2 | 数据表结构细节 |
| 只补接口和数据 | 3、4 | 非功能和研发计划 |
| 只补状态、流程、一致性、安全、测试 | 5 | 文档头和研发计划 |
| 只补非功能和研发计划 | 6 | 接口和表结构细节 |

## 1. 文档头、背景与目标模板

```markdown
# <需求/系统/模块名称> 系统分析设计文档

## 文档修正历史

| 版本 | 作者 | 日期 | 备注 |
| --- | --- | --- | --- |
| v0.1 | <作者> | <YYYY-MM-DD> | 初稿 |

## 一、需求背景

### 1.1 背景与问题

- 当前业务/系统问题：
- 影响用户/角色/系统：
- 不做的风险：
- 已知约束：合规、成本、时间、历史包袱、团队能力等。

### 1.2 术语与范围

| 术语 | 说明 |
| --- | --- |
| <术语> | <说明> |

## 二、目标

### 2.1 业务目标

- <目标 1>

### 2.2 技术目标

- 性能/容量/稳定性目标：
- 可观测/可运维目标：

### 2.3 非目标

- 本次不做：
- 后续版本再做：
```

## 2. 概要设计模板

```markdown
## 三、概要设计

### 3.1 系统边界

- 业务边界：
- 模块边界：
- 数据边界：
- 安全边界：
- 运维边界：

### 3.2 核心方案

- 方案概述：
- 核心流程：
- 关键依赖：
- 同步/异步关系：
- 失败路径：

### 3.3 方案取舍

| 方案 | 优点 | 缺点/风险 | 结论 |
| --- | --- | --- | --- |
| 方案 A |  |  |  |
```

## 3. 模块与接口设计模板

```markdown
## 四、详细设计

### 4.1 模块/类设计

| 模块/类 | 职责 | 依赖 | 禁止依赖 | 备注 |
| --- | --- | --- | --- | --- |
| <Module/Class> |  |  |  |  |

### 4.2 接口设计

#### 4.2.1 API/RPC/内部服务

| 接口 | 入参 | 出参 | 权限 | 幂等 | 错误码 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| <接口> |  |  |  |  |  |  |

#### 4.2.2 MQ/回调/任务

| 名称 | 触发条件 | 消息/参数 | 幂等键 | 失败处理 | 备注 |
| --- | --- | --- | --- | --- | --- |
| <名称> |  |  |  |  |  |
```

## 4. 数据设计模板

```markdown
### 4.3 数据设计

#### <表中文名>

表名：<table_name>

业务用途：<说明该表承载的业务事实、归属主体和写入时机>

数据归属：<说明租户、用户、账户、订单或其他业务主体>

字段清单：

| 字段名称 | 字段类型 | 是否必填 | 字段说明 | 值示例 |
| --- | --- | --- | --- | --- |
| id | bigint(20) | [X] | 主键 |  |
| gmt_create | datetime(3) | [X] | 创建时间 |  |
| gmt_modified | datetime(3) | [X] | 最后更新时间 |  |
| tenant_id | bigint(20) | [X] | 归属租户 | 1 |
| sn | varchar(50) | [X] | 业务流水号 |  |
| state | tinyint(3) | [X] | 业务状态；枚举见状态说明 | 1 |
| is_deleted | tinyint(3) | [X] | 是否删除：0 未删除，1 已删除 | 0 |
| version | int(11) | [X] | 数据版本号，乐观锁 | 1 |

状态说明：

- state = 1：<状态含义>
- state = 2：<状态含义>

唯一索引：

| 索引名称 | 索引字段 | 索引用途 |
| --- | --- | --- |
| uk_<table>_<field> | <field> | <说明唯一性约束对应的业务规则> |

普通索引：

| 索引名称 | 索引字段 | 索引用途 |
| --- | --- | --- |
| idx_<table>_<field> | <field> | <说明查询、排序或分页场景> |

数据约束与兼容：

- 新增必填字段默认值：
- 历史数据迁移：
- 删除与归档：
```

## 5. 状态、流程与专项设计模板

```markdown
### 4.4 状态机设计

| 当前状态 | 事件 | 守卫条件 | 动作 | 下一状态 | 非法流转/回滚 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

### 4.5 流程/时序设计

- 主流程：
- 异常流程：
- 补偿流程：
- 人工介入流程：
- 超时处理：

### 4.6 一致性设计

- 事务边界：
- 幂等键：
- 重试/去重：
- 补偿/对账：
- 最终一致性窗口：
- 告警与人工兜底：

### 4.7 演进与兼容设计

- 历史接口兼容：
- 历史数据兼容：
- 双写/回填/切流：
- 灰度与回滚：
- 旧能力下线标准：

### 4.8 安全设计

- 鉴权/授权：
- 租户隔离：
- 敏感信息脱敏：
- 密钥与配置：
- 审计留痕：

### 4.9 测试设计

| 测试类型 | 场景 | 输入 | 预期输出 | 红线 |
| --- | --- | --- | --- | --- |
| 单元测试 |  |  |  |  |
| 集成测试 |  |  |  |  |
| 契约测试 |  |  |  |  |
| 回归测试 |  |  |  |  |
```

## 6. 非功能、研发计划与参考资料模板

```markdown
## 五、非功能设计

### 5.1 性能与容量

- QPS/RT：
- 数据量增长：
- 慢路径：
- 热点与索引：

### 5.2 可用性与生产就绪

- 失败发现：
- 故障隔离：
- 恢复方式：
- 监控指标：
- 告警规则：
- Runbook：

### 5.3 兼容性与风险

- 新旧版本兼容：
- 配置/数据库/消息兼容：
- 技术风险：
- 残余风险：

## 六、研发计划

| 阶段 | 负责人 | 开始时间 | 结束时间 | 交付物 | 验收方式 |
| --- | --- | --- | --- | --- | --- |
| 开发 |  |  |  |  |  |
| 联调 |  |  |  |  |  |
| 提测 |  |  |  |  |  |
| 上线 |  |  |  |  |  |

## 七、参考资料

- PRD：
- ADR：
- 历史方案：
- 外部规范：
```
