# 系统分析设计模板

本文承载可复制系统分析设计模板。主规则、写作要求、评审清单和触发边界见 `system-analysis-design.md`；只有当用户要求“写系分”“系统分析设计”“详细设计”“技术详细方案”或“按团队系分模板输出”时才读取本文。

本文把产品事实推进为可编码、可测试、可发布的系统约束。正式正文按读者理解顺序组织：背景与问题 -> 目标与非目标 -> 定性与范围 -> 概要设计 -> 详细设计 -> 关键流程 -> 业务规则 -> 接口抽象 -> 非功能与风险 -> 验收摘要。内部仍按“名 -> 事 ->（图）-> 法 -> 器 -> 验”检查是否贯通，但完整性框架不等于可见目录，也不要求把追踪表和 AI 执行控制前置。

模板占位符统一使用 `〈...〉`；正式文档不得保留未替换占位符。

## 文档标识与文件命名

创建正式工程文档前，先确定规范主题、文档类型和目标路径，优先级为：用户明确指定 > 项目 `AGENTS.md` / `README.md` > 仓库已一致执行的约定 > 本模板回退规则。仓库现有文件名相互冲突时，不把单个历史文件当成规范；将冲突列为待确认并询问 owner。只有用户明确授权项目约规治理时才修改 `AGENTS.md` / `README.md`；未授权时，更新既有文档保持原路径，新建文档使用本模板回退规则并说明。

- 系统分析设计和系分统一使用 `〈主题〉-系分设计.md`，一级标题使用 `# 〈主题〉系统分析设计`。
- 架构方案、技术方案及附属设计材料使用 `〈主题〉-架构方案.md`、`〈主题〉-技术方案.md` 或 `〈主题〉-〈明确产物类型〉.md`；附属类型可以是 `代码知识图谱`、`完整场景手册` 等，不得只写模糊的“设计”或“说明”。
- 消费 Product Context Card 或产品文档时必须复用其规范主题。系分范围更窄时使用 `〈主题〉-〈子主题〉-系分设计.md`；范围扩大或业务概念变化时先退回产品 owner 确认，不自行改名吸收。
- 主题和文档类型之间只使用一个 ASCII 连字符 `-`；主题内部保留稳定领域术语和 `API`、`DSL` 等既有缩写，不用空格、下划线或中文破折号做结构分隔。
- 文件名默认不包含序号、日期、版本、`草稿`、`最终版`、`新版` 等状态；顺序、版本和状态进入索引、文档头与 Git 历史。
- 更新既有文档默认保持原路径。只有用户明确进入命名迁移任务时才改名，并同步 Markdown 链接、任务材料、脚本、测试和其他硬编码路径。

## 使用时机

- 需要直接生成完整系统分析设计文档。
- 需要按团队系分模板补齐章节。
- 需要把需求转成可评审、可编码、可验收的设计文档骨架。
- 需要承接 PRD/产品方案中的业务目标、验收种子和质量属性种子，继续推导系统边界、模块、接口、数据、测试、监控和发布门禁。

## 不适用场景

- 只做系统设计 CR、结构检查或判断是否需要系分时，先读 `system-analysis-design.md`。
- 只做架构图时，读 `diagram-output.md`。
- 只做生产上线检查时，读 `production-readiness.md`。

## 读取后必须产出

- 一份按读者主线组织、经过裁剪的系统分析设计文档；不适用的高风险内容必须说明原因，但不为完整性机械保留空表。

## 需要继续读取的 reference

- 系分写作要求和评审清单读 `system-analysis-design.md`。
- 图形化表达读 `diagram-output.md`。
- 生产风险读 `production-readiness.md`。
- 一致性读 `distributed-consistency.md`。
- 安全读 `security-architecture.md`。

## 按任务读取索引

| 任务 | 优先读取 | 跳过 |
| --- | --- | --- |
| 生成完整系分 | 正文阅读主线，按需使用 1-6 的设计组件 | 专项规则长解释和完整执行控制 |
| PRD/产品方案到系分 | 背景中的设计依据、定性与范围、概要和详细设计 | 直接从页面、技术组件、追踪表或数据库表开始设计 |
| 只补背景和目标 | 1 | 详细设计模板 |
| 只补概要设计 | 2 | 数据表结构细节 |
| 只补接口和数据 | 3、4 | 非功能和实施交接 |
| 只补状态、流程、一致性、安全、测试 | 5 | 文档头和实施交接 |
| 只补非功能、实施验证和交接 | 6 | 接口和表结构细节 |

## 正式正文的阅读主线

模板中的表格和卡片是按需组件，不是默认目录。正式系分先用正文回答“为什么这样设计、整体如何工作、细节如何承接”，再用表格比较多个模块、接口、字段或规则：

1. 背景与问题。
2. 目标与非目标。
3. 定性与范围。
4. 概要设计。
5. 详细设计。
6. 关键流程。
7. 业务规则。
8. 接口抽象与契约。
9. 非功能、风险与待确认。
10. 验收摘要与执行计划引用。

验收摘要放在正文末尾，只说明系统必须保护的业务结果、不变量、质量目标、关键边界和红线。详细验收矩阵、AC 与测试映射、验证命令、执行 owner 和任务状态进入执行计划；设计视图清单、业务驱动追踪表、质量属性场景明细和 Engineering Handoff 也默认随执行计划维护，只有项目约规要求单文档追踪时才作为末尾附录。

## 1. 文档头、背景与目标模板

```markdown
# 〈规范主题〉系统分析设计

## 文档状态与版本信息

- 当前版本：v〈版本号〉
- 文档状态：Draft / Review / Approved / Superseded
- owner：〈负责人〉
- 发布日期：〈YYYY-MM-DD〉
- 评审结论摘要：〈通过 / 待补齐 / 暂停，并说明当前有效结论〉
- 影响范围：〈涉及团队、系统、模块、接口或数据〉
- 过程记录链接：〈评审报告 / ADR / Decision Log / 任务计划 / 中间任务文档〉

说明：本系分正文只保留当前有效设计、取舍、风险、待确认、验收摘要和执行计划链接；讨论过程、迭代草稿、AI 推理轨迹、被拒方案和完整修订流水放入过程记录链接，不复制进正文。

## 一、需求背景

### 1.1 背景与问题

- 当前业务/系统问题：
- 影响用户/角色/系统：
- 不做的风险：
- 已知约束：合规、成本、时间、历史包袱、团队能力等。

### 1.2 术语与范围

| 术语 | 说明 |
| --- | --- |
| 〈术语〉 | 〈说明〉 |

### 1.3 设计依据

- 上游产品文档 / Product Context：〈可读链接与适用版本〉
- 已确认业务事实：〈目标、主体、对象、流程、规则和验收摘要〉
- 源码 / 接口 / DDL / 运行证据：〈当前系统事实〉
- 假设与待确认：〈缺口、影响、确认 owner 和未确认前处理〉

## 二、目标

### 2.1 业务目标

- 〈目标 1〉

### 2.2 技术目标

- 性能/容量/稳定性目标：
- 可观测/可运维目标：

### 2.3 非目标

- 本次不做：
- 后续版本再做：

### 2.4 定性与范围

- 问题定性：〈新增能力 / 既有能力增强 / 架构治理 / 迁移重构 / 可靠性修复等〉
- 系统职责：〈本系统必须承担什么，不承担什么〉
- 能力提供者视角：〈系统、模块和接口稳定提供什么能力，不按需求条目一一复制什么实现〉
- 共同目标、对象、不变量和契约：〈跨用例可以共同承载的工程事实〉
- 场景差异与变化轴：〈有证据的主体、渠道、法域、时点、协议或外部依赖差异如何承载〉
- 稳定点与变化点：〈不变量与真实变化轴〉
- 影响范围：〈模块、契约、数据、外部系统和团队〉
- 设计方向：〈总体判断及其事实依据〉
- 可信度：〈已确认 / 假设 / 待确认，并说明影响〉

共性优先收敛为稳定职责和简单契约；特殊性只有存在业务事实、失败差异或独立验收时才进入规则、参数、策略、适配器或明确分支。一次性差异不提前平台化。
```

## 2. 概要设计模板

```markdown
## 三、概要设计

### 3.1 方案概述

用 1-3 段说明系统如何承接产品目标、为什么采用该方案、整体运行方式和最关键的工程约束。不要只列组件名。

### 3.2 系统边界

- 业务边界：
- 模块边界：
- 数据边界：
- 安全边界：
- 运维边界：

### 3.3 核心抽象与职责

- 核心对象 / 不变量：
- 能力与模块职责：
- 依赖方向与禁止依赖：
- 稳定接口抽象：
- 外部系统与关键依赖：

### 3.4 关键流程概览

- 业务入口：
- 主路径：
- 同步 / 异步关系：
- 状态变化与副作用：
- 失败、补偿与人工路径：

### 3.5 方案取舍

先用正文说明真正存在的分歧和选择理由；只有多个方案需要横向比较时才使用表格。

| 方案 | 适用条件 | 优点 | 缺点 / 风险 | 结论与复审条件 |
| --- | --- | --- | --- | --- |
| 方案 A |  |  |  |  |

设计视图按复杂度选择，不是完整系分的固定配图任务。轻量、单边界且文字足以明确关系时写“无需单独画图”；多角色、多模块、多状态或跨系统时选择上下文、组件、运行时、状态或部署视图中的最小集合。图不能替代规则说明或验证方向，图与产品事实、契约或源码冲突时先修正图。设计视图清单和业务驱动追踪表默认进入执行计划或按需附录，不在概要设计开头打断阅读。
```

## 3. 模块与接口设计模板

```markdown
## 四、详细设计

### 4.1 模块/类设计

先用正文说明模块为什么存在、承接哪条业务流程或不变量、与相邻模块如何分工。多个模块需要比较时再使用表格。

| 模块/类 | 职责 | 依赖 | 禁止依赖 | 备注 |
| --- | --- | --- | --- | --- |
| 〈Module/Class〉 |  |  |  |  |

### 4.2 接口抽象与契约

先定义稳定接口抽象：服务对象、职责边界、输入前提、业务输出、副作用、失败语义和调用方责任；再按实际协议展开 API、RPC、内部服务、MQ、回调或任务。不得从现有 Controller、DTO 或表字段反推一个没有业务职责的接口。

#### 4.2.1 API/RPC/内部服务

| 接口 | 业务职责 | 入参 | 出参 | 权限 | 幂等 | 错误码 / 失败语义 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 〈接口〉 |  |  |  |  |  |  |  |

#### 4.2.2 MQ/回调/任务

| 名称 | 触发条件 | 消息/参数 | 幂等键 | 失败处理 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 〈名称〉 |  |  |  |  |  |
```

## 4. 数据设计模板

涉及新增或调整数据表时使用本节；无表变更时写明“不适用”及依据。普通项目先遵循本地数据库规范；项目声明、依赖或源码上下文命中 Wind/Nobe 专项时，必须包含 `id`、`gmt_create`、`gmt_modified`，并按需选择 `creator`、`modifier`、`order_index`。`tenant_id`、`sn`、`state`、`is_deleted`、`version` 仍须有真实业务依据，不机械加入所有表。

```markdown
### 4.3 数据设计

#### 〈表中文名〉

表名：〈table_name〉

业务用途：〈说明该表承载的业务事实、归属主体和写入时机〉

数据归属：〈说明租户、用户、账户、订单或其他业务主体〉

数据库约规来源：〈项目 AGENTS.md / 数据库规范 / 既有同类表；无明确规范时写待确认 owner〉

主键策略：〈主键字段、生成方式、是否外部暴露及选择依据〉

业务唯一性：〈对应业务规则及数据库唯一键；不存在业务唯一性时说明原因〉

DDL / 迁移脚本：〈文件路径；未生成时说明 owner 和生成时点〉

字段清单：

| 字段名称 | 字段类型与精度 | 是否必填 | 默认值 | 变更类型 | 字段说明 | 业务约束 | 值示例 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 〈field_name〉 | 〈数据库类型、长度或精度〉 | 是/否 | 〈无/NULL/具体值〉 | 〈新表字段/新增字段/调整字段/既有字段〉 | 〈稳定业务语义〉 | 〈主键/业务唯一/枚举/金额币种精度/敏感级别等〉 | 〈示例〉 |

命中 Wind/Nobe 专项的项目将以下字段并入字段清单：`id bigint(20)`、`gmt_create datetime`、`gmt_modified datetime` 为强制；`creator`、`modifier` 类型遵循项目约规并按需使用；`order_index int(11)` 按需使用。

新增必填字段必须给兼容迁移方案；默认值、分阶段回填或暂时允许为空按真实业务语义和数据库变更能力选择，不得为了通过 DDL 虚构业务默认值。金额、时间、状态、敏感数据和外部协议字段必须写清单位、精度、时区、枚举、脱敏或映射口径。

状态说明（存在状态字段时填写）：

- state = 1：〈状态含义〉
- state = 2：〈状态含义〉

唯一索引：

| 索引名称 | 索引字段（有序） | 对应业务规则 | 冲突处理 |
| --- | --- | --- | --- |
| uk_〈table〉_〈field〉 | 〈field_a, field_b〉 | 〈说明数据库唯一键保护的业务唯一性〉 | 〈拒绝/返回既有记录/其他明确语义〉 |

普通索引：

| 索引名称 | 索引字段（有序） | 查询/排序场景 | 索引依据 |
| --- | --- | --- | --- |
| idx_〈table〉_〈field〉 | 〈field_a, field_b〉 | 〈查询条件、排序和分页方式〉 | 〈数据量、选择性或执行计划证据〉 |

数据约束与兼容：

- 新增必填字段默认值：
- 历史数据迁移：
- 改名、改类型和删除字段的兼容发布：
- 数据校验与回滚：
- 删除与归档：
- 逻辑关联、外键策略和一致性维护责任：
- 敏感数据、加密、脱敏和访问边界：
```

## 5. 状态、流程与专项设计模板

```markdown
### 4.4 状态机设计

| 当前状态 | 事件 | 守卫条件 | 动作 | 下一状态 | 非法流转/回滚 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

### 4.5 运行时场景与流程/时序设计

- 业务入口：
- 参与模块：
- 主流程：
- 异常流程：
- 补偿流程：
- 人工介入流程：
- 超时处理：

### 4.5A 状态与工程规则

- 核心不变量：
- 状态迁移与非法流转：
- 输入、前置条件和输出：
- 权限与安全规则：
- 错误、重复、超时和状态不明处理：

### 4.6 一致性设计

- 事务边界：
- 幂等键：
- 重试/去重：
- 补偿/对账：
- 最终一致性窗口：
- 告警与人工兜底：

### 4.7 演进与兼容设计

- 历史接口兼容：
- 历史数据兼容：
- 双写/回填/切流：
- 灰度与回滚：
- 旧能力下线标准：

### 4.8 安全设计

- 鉴权/授权：
- 租户隔离：
- 敏感信息脱敏：
- 密钥与配置：
- 审计留痕：

### 4.9 验证设计摘要

正文只说明哪些设计事实需要由测试、观测或人工评审保护，以及为什么选择这些验证层级：

- 可代码化：〈核心规则、状态、不变量、接口契约和失败语义〉
- 可观测化：〈指标、日志、链路、告警、报表、对账和 SLO〉
- 可评审化：〈产品、运营、财务、合规、安全或上线确认〉
- 验证重点：〈最容易出错、后果最高或最难回滚的路径〉

第一批失败测试、详细测试矩阵、AC 与测试映射、验证命令、执行 / 验证 owner 和状态进入执行计划，不在详细设计中展开。

### 4.10 规则落地表

规则是产品事实与工程实现之间的承接点。先在正文说明规则如何影响对象、状态、模块和契约；多规则、高风险或跨团队场景再用一张表横向核对，不另建全量追踪台账：

| 规则ID | 业务表达 | 对象 / 状态 | 工程承接 | 失败语义 | owner |
| --- | --- | --- | --- | --- | --- |
| R-001 | 〈允许、禁止或必须保持的业务事实〉 | 〈对象与状态〉 | 〈模块、守卫、接口、事件或数据约束〉 | 〈拒绝、补偿、告警或人工处理〉 | 〈产品 / 研发 / 运营〉 |
```

## 6. 非功能、风险与执行计划引用模板

```markdown
## 五、非功能设计

### 5.1 性能与容量

- QPS/RT：
- 数据量增长：
- 慢路径：
- 热点与索引：

### 5.2 可用性与生产就绪

- 失败发现：
- 故障隔离：
- 恢复方式：
- 监控指标：
- 告警规则：
- Runbook：

### 5.2A 质量属性场景

正文概括最关键的质量属性、业务原因、触发条件、期望响应和度量口径。多个场景需要追踪时，质量属性ID、受影响资产、验证资产和 owner 的明细表进入执行计划或按需附录。

### 5.3 兼容性与风险

- 新旧版本兼容：
- 配置/数据库/消息兼容：
- 技术风险：
- 残余风险：

## 六、风险、验收摘要与执行计划引用

验收摘要放在正文末尾，使用架构、产品和工程评审者都能理解的语言：

- 业务结果与系统不变量：〈本设计必须保护什么〉
- 公共契约与数据边界：〈哪些接口、事件、字段或状态不能被破坏〉
- 质量目标：〈性能、可靠性、一致性、安全、可观测和可运营目标〉
- 关键边界与异常：〈失败、重复、超时、并发、外部不可用和人工接管〉
- 风险与待确认：〈影响、owner 和未确认前处理〉
- 执行计划：〈权威路径、适用版本和状态入口〉

详细验收矩阵、AC 与测试映射、验证命令、执行 owner 和任务状态进入执行计划。第一实施切片、写入 / 只读范围、禁止事项、失败测试、灰度、回滚、停止条件和 Engineering Handoff 也由执行计划承载；系分只引用其权威路径，不复制一份容易漂移的任务账本。Engineering Handoff 由知止者编排，不是 Execution Grant、测试通过、Git 授权或上线批准。

## 七、参考资料与证据索引
| 类型 | 资料名称 | 链接或路径 | 版本/日期 | 用途与核验状态 |
| --- | --- | --- | --- | --- |
| 〈PRD/Product Context/ADR/源码/测试/日志/指标/历史方案/外部规范〉 | 〈可读名称〉 | 〈Markdown 链接、仓库路径或源码锚点〉 | 〈版本、发布日期或读取日期〉 | 〈支撑章节或设计结论；已读取/待核验〉 |
外部文档使用可读 Markdown 链接，不只粘贴裸 URL。引用标识为可选；只有正文多次引用、规则或验证证据需要反向追踪、资料容易混淆或存在审计要求时，才增加 `REF-001` 等稳定标识。轻量系分来源已在设计依据和正文中清楚标注时，可不重复展开本表；AI 推理、讨论草稿和被拒方案进入过程资产。

按需追踪附录仅在法规、审计、合同或项目约规要求单文档追踪时启用，可包含：设计视图清单、业务驱动追踪表、质量属性场景明细、规则落地表、验收种子与验证资产映射。普通项目把这些内容放入执行计划，正式系分只保留结论和链接。
```
