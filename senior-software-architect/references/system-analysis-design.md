# 系统分析设计约规

本文基于团队《系分模板》整理，用于架构师输出系统分析设计、设计评审和可编码方案。系分文档不是形式材料，而是把需求背景、目标、边界、方案、风险、计划和验证统一到一份可执行设计中。

## 按任务读取索引

| 任务 | 优先读取 | 跳过 |
| --- | --- | --- |
| 快速判断是否需要系分 | 1、2、5 | 3.0 长模板 |
| 生成完整系统分析设计 | 3.0、3.1-3.8、4、5 | 只与当前项目无关的专项说明 |
| 详细设计到可编码状态 | 3.5、3.5.1、3.6、3.7 | 文档修正历史模板 |
| 表结构、索引、DDL 评审 | 3.5.1、5、6 | 图形要求和研发计划细节 |
| 生产风险、兼容、灰度回滚 | 3.6、3.7、5、7 | 表结构模板细节 |
| 设计图输出 | 4，并按需读 `diagram-output.md` | 长模板中非图形章节 |

## 目录

- `1. 使用时机`
- `2. 文档结构`
- `3. 章节写作要求`
- `4. 设计图要求`
- `5. 评审清单`
- `6. 常见反模式`
- `7. 与其他规范的关系`

## 1. 使用时机

以下场景必须优先使用本约规：

- 新增核心业务能力、跨模块能力、公共平台能力或中间件/SDK 产品能力。
- 涉及服务边界、模块边界、数据模型、状态机、权限、安全、资金、库存、账务、审计等高风险设计。
- 涉及性能、容量、可用性、兼容性、灰度、回滚、应急或生产运维风险。
- 需求需要跨产品、研发、测试、运维、架构或外部系统协同时。

仅小修文案、局部样式、低风险配置或单点 Bug 修复可不写完整系分，但仍需在 PR 中说明背景、影响、验证和回滚。

正式、完整、可评审、提交前、CR 或触发验证场景中，系统分析设计必须运行 `scripts/check_architecture_deliverable.py --kind system-design` 检查本地文本或显式传入的本地文件。该脚本只检查背景目标、边界、概要设计、详细设计、流程状态、非功能、测试和研发计划是否齐全，不联网、不写文件、不替代架构评审或工程验证；无法运行时必须说明原因、人工检查结果和残余风险。

## 2. 文档结构

```text
文档修正历史
一、需求背景
二、目标
三、概要设计
四、详细设计
五、非功能设计
六、研发计划
七、参考资料
```

推荐微调：

- 在“目标”中补充“非目标”，避免需求膨胀。
- 在“概要设计”中明确系统边界、依赖边界和核心取舍。
- 在“详细设计”中补齐接口、数据、状态、异常、幂等、事务和测试验收。
- “非功能设计”中技术风险应视为必填，尤其是可监控、可灰度、可应急。

## 3. 章节写作要求

### 3.0 可复制系分模板

当用户要求“写系分”“系统分析设计”“详细设计”“技术详细方案”或“按团队系分模板输出”时，优先使用以下模板，再按项目实际裁剪。不适用章节必须写明“不适用原因”，不要直接删除高风险章节。

```markdown
# <需求/系统/模块名称> 系统分析设计文档

## 文档修正历史

| 版本 | 作者 | 日期 | 备注 |
| --- | --- | --- | --- |
| v0.1 | <作者> | <YYYY-MM-DD> | 初稿 |

## 一、需求背景

### 1.1 背景与问题

- 当前业务/系统问题：
- 影响用户/角色/系统：
- 不做的风险：
- 已知约束：合规、成本、时间、历史包袱、团队能力等。

### 1.2 术语与范围

| 术语 | 说明 |
| --- | --- |
| <术语> | <说明> |

## 二、目标

### 2.1 业务目标

- <目标 1>

### 2.2 技术目标

- 性能/容量/稳定性目标：
- 可观测/可运维目标：

### 2.3 非目标

- 本次不做：
- 后续版本再做：

## 三、概要设计

### 3.1 系统边界

- 业务边界：
- 模块边界：
- 数据边界：
- 安全边界：
- 运维边界：

### 3.2 核心方案

- 方案概述：
- 核心流程：
- 关键依赖：
- 同步/异步关系：
- 失败路径：

### 3.3 方案取舍

| 方案 | 优点 | 缺点/风险 | 结论 |
| --- | --- | --- | --- |
| 方案 A |  |  |  |

## 四、详细设计

### 4.1 模块/类设计

| 模块/类 | 职责 | 依赖 | 禁止依赖 | 备注 |
| --- | --- | --- | --- | --- |
| <Module/Class> |  |  |  |  |

### 4.2 接口设计

#### 4.2.1 API/RPC/内部服务

| 接口 | 入参 | 出参 | 权限 | 幂等 | 错误码 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| <接口> |  |  |  |  |  |  |

#### 4.2.2 MQ/回调/任务

| 名称 | 触发条件 | 消息/参数 | 幂等键 | 失败处理 | 备注 |
| --- | --- | --- | --- | --- | --- |
| <名称> |  |  |  |  |  |

### 4.3 数据设计

#### <表中文名>

表名：<table_name>

业务用途：<说明该表承载的业务事实、归属主体和写入时机>

数据归属：<说明租户、用户、账户、订单或其他业务主体>

字段清单：

| 字段名称 | 字段类型 | 是否必填 | 字段说明 | 值示例 |
| --- | --- | --- | --- | --- |
| id | bigint(20) | [X] | 主键 |  |
| gmt_create | datetime(3) | [X] | 创建时间 |  |
| gmt_modified | datetime(3) | [X] | 最后更新时间 |  |
| tenant_id | bigint(20) | [X] | 归属租户 | 1 |
| sn | varchar(50) | [X] | 业务流水号 |  |
| state | tinyint(3) | [X] | 业务状态；枚举见状态说明 | 1 |
| is_deleted | tinyint(3) | [X] | 是否删除：0 未删除，1 已删除 | 0 |
| version | int(11) | [X] | 数据版本号，乐观锁 | 1 |

状态说明：

- state = 1：<状态含义>
- state = 2：<状态含义>

唯一索引：

| 索引名称 | 索引字段 | 索引用途 |
| --- | --- | --- |
| uk_<table>_<field> | <field> | <说明唯一性约束对应的业务规则> |

普通索引：

| 索引名称 | 索引字段 | 索引用途 |
| --- | --- | --- |
| idx_<table>_<field> | <field> | <说明查询、排序或分页场景> |

数据约束与兼容：

- 新增必填字段默认值：
- 历史数据迁移：
- 删除与归档：

### 4.4 状态机设计

| 当前状态 | 事件 | 守卫条件 | 动作 | 下一状态 | 非法流转/回滚 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

### 4.5 流程/时序设计

- 主流程：
- 异常流程：
- 补偿流程：
- 人工介入流程：
- 超时处理：

### 4.6 一致性设计

- 事务边界：
- 幂等键：
- 重试/去重：
- 补偿/对账：
- 最终一致性窗口：
- 告警与人工兜底：

### 4.7 演进与兼容设计

- 历史接口兼容：
- 历史数据兼容：
- 双写/回填/切流：
- 灰度与回滚：
- 旧能力下线标准：

### 4.8 安全设计

- 鉴权/授权：
- 租户隔离：
- 敏感信息脱敏：
- 密钥与配置：
- 审计留痕：

### 4.9 测试设计

| 测试类型 | 场景 | 输入 | 预期输出 | 红线 |
| --- | --- | --- | --- | --- |
| 单元测试 |  |  |  |  |
| 集成测试 |  |  |  |  |
| 契约测试 |  |  |  |  |
| 回归测试 |  |  |  |  |

## 五、非功能设计

### 5.1 性能与容量

- QPS/RT：
- 数据量增长：
- 慢路径：
- 热点与索引：

### 5.2 可用性与生产就绪

- 失败发现：
- 故障隔离：
- 恢复方式：
- 监控指标：
- 告警规则：
- Runbook：

### 5.3 兼容性与风险

- 新旧版本兼容：
- 配置/数据库/消息兼容：
- 技术风险：
- 残余风险：

## 六、研发计划

| 阶段 | 负责人 | 开始时间 | 结束时间 | 交付物 | 验收方式 |
| --- | --- | --- | --- | --- | --- |
| 开发 |  |  |  |  |  |
| 联调 |  |  |  |  |  |
| 提测 |  |  |  |  |  |
| 上线 |  |  |  |  |  |

## 七、参考资料

- PRD：
- ADR：
- 历史方案：
- 外部规范：
```

### 3.1 文档修正历史

记录版本、作者、备注和修订日志。每次评审后修改必须能追溯原因，避免口头共识丢失。

### 3.2 需求背景

必须回答“为什么做这件事”：

- 当前业务问题、客户问题或系统问题是什么。
- 涉及哪些用户、角色、系统、模块和外部依赖。
- 不做会带来什么业务、技术、数据、安全或运维风险。
- 已知约束是什么，例如合规、成本、时间、团队能力、历史包袱。

### 3.3 目标

目标必须能约束设计和编码，不只是复述需求。

- **业务目标**：解决什么问题，带来什么业务价值，影响哪些角色。
- **技术目标**：容量、吞吐、RT、CPU、内存、成功率、错误率、资源成本等可量化指标。
- **系统目标**：建设什么系统能力，例如幂等、对账、补偿、审计、灰度、扩展点。
- **非目标**：本次明确不做什么，哪些能力留到后续版本，哪些边界不扩张。

### 3.4 概要设计

概要设计负责说明主体思路、系统边界和关键取舍。

- 用上下文图、用例图、组件图或流程图说明系统与用户、外部系统、内部模块的关系。
- 明确业务边界、模块边界、服务边界、数据边界、安全边界和运维边界。
- 说明核心流程、关键依赖、同步/异步关系、读写路径和失败路径。
- 对至少一个备选方案做取舍说明，解释为什么采用当前方案。
- 明确当前方案是否需要兼容历史数据、历史接口、历史客户端或新旧版本共存。

### 3.5 详细设计

详细设计必须达到可编码状态。不能只停留在“这里调用某服务”“这里入库”。

- **模块/类设计**：职责、依赖方向、扩展点、禁止依赖、主要类和方法边界。
- **接口设计**：API、RPC、MQ、回调、任务、导入导出的入参、出参、错误码、幂等和权限。
- **数据设计**：表结构、字段语义、索引、唯一约束、状态字段、审计字段、迁移和归档。
- **状态机设计**：状态、事件、守卫条件、动作、终态、非法流转和回滚流转。
- **流程/时序设计**：主流程、异常流程、补偿流程、人工介入流程和超时处理。
- **一致性设计**：事务边界、幂等键、重试、去重、补偿、对账、最终一致性窗口；复杂场景见 `distributed-consistency.md`。
- **演进设计**：涉及遗留系统、历史接口、历史数据或核心链路替换时，说明防腐层、契约测试、双写/回填/切流、灰度和旧能力下线标准；详细检查见 `evolutionary-architecture.md`。
- **安全设计**：鉴权、授权、租户隔离、敏感信息脱敏、密钥管理和审计留痕；复杂场景见 `security-architecture.md`。
- **测试设计**：单测、集成测试、契约测试、回归测试、压测和验收场景。

#### 3.5.1 数据表结构设计模板

详细设计涉及新增或调整数据表时，必须给出可编码、可评审、可生成 DDL 的表结构说明。不要只写“新增订单表”“字段见代码”。

每张表至少包含：

- 表中文名、表名、业务用途和数据归属。
- 字段清单：字段名称、字段类型、是否必填、字段说明、值示例。
- 主键、唯一索引、普通索引和索引字段顺序。
- 状态字段枚举、金额字段单位、币种字段、租户/用户归属、逻辑删除、乐观锁和审计字段。
- 数据生命周期：创建、更新、归档、清理、软删除、历史数据兼容和迁移策略。

表结构模板（字段一行一个，索引一行一个；表名、字段、索引名称和值示例不使用加粗或代码背景）：

##### <表中文名>

表名：<table_name>

业务用途：<说明该表承载的业务事实、归属主体和写入时机>

数据归属：<说明租户、用户、账户、订单或其他业务主体>

字段清单：

| 字段名称 | 字段类型 | 是否必填 | 字段说明 | 值示例 |
| --- | --- | --- | --- | --- |
| id | bigint(20) | [X] | 主键 |  |
| gmt_create | datetime(3) | [X] | 创建时间 |  |
| gmt_modified | datetime(3) | [X] | 最后更新时间 |  |
| tenant_id | bigint(20) | [X] | 归属租户 | 1 |
| user_id | bigint(20) | [ ] | 归属用户；无用户归属时说明原因 | 10001 |
| sn | varchar(50) | [X] | 业务流水号，全局或业务域内唯一 | ORD202601010001 |
| state | tinyint(3) | [X] | 业务状态；枚举见下方状态说明 | 1 |
| amount | int(11) | [X] | 金额，单位：分 | 100 |
| currency | varchar(10) | [X] | 币种，使用 ISO 4217 或项目统一枚举 | USD |
| is_deleted | tinyint(3) | [X] | 是否删除：0 未删除，1 已删除 | 0 |
| version | int(11) | [X] | 数据版本号，乐观锁 | 1 |

状态说明：

- state = 1：<状态含义>
- state = 2：<状态含义>

唯一索引：

| 索引名称 | 索引字段 | 索引用途 |
| --- | --- | --- |
| uk_<table>_<field> | <field> | <说明唯一性约束对应的业务规则> |

普通索引：

| 索引名称 | 索引字段 | 索引用途 |
| --- | --- | --- |
| idx_<table>_<field> | <field> | <说明查询、排序或分页场景> |

数据约束与兼容：

- 新增必填字段默认值：<说明默认值或兼容策略>
- 历史数据迁移：<是否需要回填、双写、灰度或兼容读取>
- 删除与归档：<逻辑删除、归档周期、清理策略>

表设计约规：

- 【强制】表名、字段名使用小写字母、数字和下划线，禁止数字开头。
- 【强制】表名、字段名称、索引名称和值示例使用纯文本展示，不加粗，不使用代码背景。
- 【强制】字段必须一行一个，索引必须一行一个；不得把多个字段或多个索引合并到同一行。
- 【强制】字段类型尽量写明长度、精度或时间精度，例如 varchar(50)、bigint(20)、tinyint(3)、int(11)、decimal(18,6)、datetime(3)。
- 【强制】表必须包含 id、gmt_create、gmt_modified；涉及多租户、用户归属、逻辑删除或乐观锁时，应显式说明 tenant_id、user_id、is_deleted、version 是否需要。
- 【强制】业务唯一约束必须使用数据库唯一键保证，不能只依赖应用层判断。
- 【强制】禁止数据库外键和级联删除，外键关系在应用层维护；设计文档中仍要说明逻辑关联字段和一致性维护方式。
- 【强制】新增必填字段必须有默认值，否则字段应允许为空并配合兼容逻辑。
- 【强制】删除字段、改字段名、改字段类型必须先做兼容发布，再执行 DDL。
- 【强制】金额字段必须说明单位、精度、币种和正负语义；不允许只写 amount 而不说明业务含义。
- 【强制】状态字段必须给出枚举值、含义、终态和非法流转；复杂状态见状态机设计。
- 【推荐】高频查询条件、租户隔离字段、业务流水号、状态流转查询和排序字段必须评估索引。
- 【推荐】索引设计要说明唯一性、字段顺序、选择性、分页/排序场景和是否会影响写入性能。
- 【推荐】text、json、快照、凭证、外部返回信息等大字段应说明用途、大小风险、脱敏要求和是否参与查询。
- 【推荐】表结构说明应能反向支撑 Entity、DTO、Query、Mapper、迁移脚本、测试数据和代码生成。

### 3.6 非功能设计

非功能设计默认必填；确实不适用时需要说明原因。

- **性能考虑**：QPS、RT、慢路径、热点、缓存、索引、批量处理、线程池和连接池。
- **容量考虑**：并发、CPU、内存、带宽、磁盘、数据库连接、消息积压和数据增长。
- **可用性考虑**：哪些情况会失败，如何发现，如何隔离，如何恢复，自动恢复还是人工恢复。
- **兼容性考虑**：API、DTO、消息、数据库、配置、新旧版本、老数据和外部依赖兼容。
- **生产就绪**：SLO、容量基线、灰度指标、回滚阈值、Runbook 和应急负责人；详细检查见 `production-readiness.md`。
- **技术风险**：可监控、可灰度、可应急必须说明；高风险项给出负责人和验证方式。
- **可运维性**：安装、部署、升级、排障、开关、日志、指标、链路、告警和 Runbook。

### 3.7 研发计划

计划必须能支撑协作和风险控制：

- 开发负责人、质量负责人、联调负责人和上线负责人。
- 开发开始/结束时间、联调时间、提测时间、预发时间、生产发布时间。
- 关键里程碑、前置依赖、阻塞项、灰度节奏、回滚窗口和验收负责人。
- 对高风险任务拆分阶段交付，避免一次性大爆炸上线。

### 3.8 参考资料

列出 PRD、接口文档、架构图、历史方案、ADR、外部规范、开源文档和相关工单。引用材料应能支撑方案判断，而不是堆链接。

## 4. 设计图要求

- 用例图：说明用户、角色、系统边界和可见能力。
- 上下文图：说明本系统与外部系统、内部平台、第三方依赖的关系。
- 组件图：说明模块职责、依赖方向和可替换边界。
- 流程图：说明主流程、异常流程、人工介入和回滚路径。
- 时序图：说明跨系统、消息、回调、任务和事务边界。
- 状态机图：说明状态、事件、守卫条件、动作和非法流转。

每张图必须有图前问题和图后结论。图中术语必须与代码、接口、数据库、测试和业务文档一致。

## 5. 评审清单

- 背景是否说明为什么做、影响谁、不做的风险是什么。
- 目标是否覆盖业务目标、技术目标、系统目标和非目标。
- 概要设计是否明确边界、依赖、核心流程和关键取舍。
- 关键取舍是否有备选方案、放弃理由和复审条件；重大决策是否需要 ADR。
- 详细设计是否达到可编码状态，接口、数据、状态、异常、幂等和测试是否完整。
- 数据表结构是否包含表中文名、表名、字段清单、必填性、字段语义、枚举、金额单位、索引、唯一约束、迁移兼容和归档策略。
- 表设计是否遵循数据库约规：通用字段、命名、唯一键、禁止外键级联、必填字段默认值、DDL 兼容发布、索引和查询场景。
- 一致性设计是否覆盖业务不变量、事务边界、幂等、去重、补偿、对账、告警和人工兜底。
- 遗留系统或迁移方案是否覆盖防腐层、契约测试、双写/回填/切流、灰度、回滚和下线条件。
- 安全设计是否覆盖认证、授权、租户隔离、敏感数据、密钥、审计和安全测试。
- 非功能是否覆盖性能、容量、可用性、兼容性、监控、灰度、应急和运维。
- 生产就绪是否覆盖 SLO、容量、压测、开关、监控、Runbook、回滚阈值和残余风险。
- 是否明确新旧版本、老数据、老接口、老客户端和外部依赖兼容策略。
- 是否有灰度、切流、开关、回滚、告警和人工兜底。
- 研发计划是否有负责人、时间点、里程碑、阻塞项和验收方式。
- 参考资料是否足以支撑设计判断和后续追溯。

## 6. 常见反模式

- 背景只写需求名称，不说明问题、影响和风险。
- 目标不可衡量，导致设计和编码无法判断是否偏航。
- 概要设计只有组件名，没有边界、依赖和取舍。
- 详细设计没有接口、数据、状态机、错误码和测试场景。
- 表结构只给 DDL 或只给字段名，不说明字段语义、必填性、枚举、金额单位、索引依据和兼容迁移。
- 非功能设计写“无”，但实际涉及容量、兼容、上线、监控或回滚风险。
- 只写正常流程，不写异常流程、补偿、人工介入和应急开关。
- 研发计划只有日期，没有负责人、依赖、里程碑和验收标准。

## 7. 与其他规范的关系

- `architecture.md`：定义架构职责、核心抽象、质量属性和架构评审关注点。
- `product-design.md`：定义产品目标、能力地图、业务规则和面向开发/测试的表达。
- `review-and-output-templates.md`：提供架构方案、实施计划、生产变更和兼容性治理模板。
- `production-readiness.md`：定义 SLO、容量、灰度、监控、数据变更、外部依赖、Runbook 和回滚要求。
- `adr-and-tradeoff.md`：定义重大决策和技术取舍的 ADR 结构。
- `distributed-consistency.md`：定义分布式一致性、消息、幂等、补偿和对账要求。
- `evolutionary-architecture.md`：定义遗留系统改造、迁移、切流和微服务拆分门槛。
- `security-architecture.md`：定义认证授权、租户隔离、敏感数据、密钥、审计和安全测试。
- `workflow.md`：定义代码修改、验证命令、PR 和 Git 流程。
- `testing.md`：定义如何把验收标准沉淀为测试资产。
