# 视觉设计单

按任务裁剪并删除无关章节。模板完整不等于设计完整，不得为填表发明故事事实、史实或对象属性。

制作状态：视觉候选 / 已确认设计 / 已生成待验收 / 已验收图像

采用状态：属性候选 / 作者采用 / 正典写回

任务模式：独立设计 / novelist 交接 / 现有设计审查 / 已确认器物成图 / 生成结果验收

## 权威与边界

作品或项目：

设计对象：

权威来源与版本：

继承的已确认事实：

必须保留：

允许改变：

未知或待确认：

目标载体：

输出用途：预览 / 项目资产

尺度参照：

## 人物专项（按需）

当前图片角色：审美基准图 / 身份锚图 / 服饰状态图 / 场景表现图

适用人物、阶段、场景与用途：

外貌与体态：已确认 / 允许探索 / 未知

发式、发饰与连接：

衣装结构、材料、维护与携物接口：

关系、具体意图、动作与空间选择：

当前参考图及其作用：身份 / 审美 / 服饰 / 动作 / 材质 / 构图

### 逐属性采用矩阵

| 图片 ID 或路径 | 属性 | 图片角色 | 结论 | 可见证据 | 后续效力 |
| --- | --- | --- | --- | --- | --- |
|  | 按任务填写年龄感、气质、配色、轮廓、脸、体态、发式、衣制、饰物或动作中的承重项 |  | 采用 / 保留候选 / 拒绝 / 无法验证 |  | 可作为后续约束 / 不得继承 / 需要补图或确认 |

## 设计结论

一句话说明采用方向、当前状态及其服务的叙事或视觉任务。

方向词 -> 可见证据 -> 禁用捷径：

## 视觉母题

材质语法：

结构语法：

母题及使用边界：

## 东方审美与造物语言（按需）

主视觉命题与第一观看关系：

形与势（主从 / 重心 / 开合 / 曲直 / 疏密 / 藏露）：

纹饰（语境 / 单元 / 组织 / 部位 / 结构关系 / 工艺 / 含义边界）：

颜色（材料基底 / 来源工艺 / 主辅点底 / 明度纯度冷暖 / 邻接 / 状态）：

留白（位置 / 围合 / 承载内容 / 作用 / 失效方式）：

意境链（观察者 / 对象 / 空间 / 时间 / 天候 / 动作 / 未显之物）：

文字与道教图形分型及证据边界：

营造或造器（结构 / 工序 / 接合 / 使用 / 维护 / 失效）：

本轮禁用的传统元素捷径：

## 五轴设计

### 叙事身份

### 结构与功能

### 材质与工艺

### 环境与关系

### 状态与连续性

## 神异、载文与纹样工器（按需）

器类、职事与上游规则指针：

神话 / 历史实物 / 建筑营造 / 工器制作证据：

器骨、部件、接合与配套接口：

材料、工具、成形、修整、校准与修复痕迹：

载文类型、内容、载体、制作方式、可读者与权限：

纹样母题、基本单元、组织方式、适用部位、载体工艺、使用资格与磨损：

正反面 / 部位语义映射：

力量来源、器身载体、运行路径、触发、阻断与余波：

常态、发动、过载、受损、修复或重铸状态：

状态色场（基底 / 轴线或锚点 / 分布 / 连续性与对称性 / 边缘 / 外放与光晕 / 禁区）：

## 不可变锚点与允许变化

当前锁定层与已确认层：

不可变锚点：

可变状态：

一次性效果：

状态变化原因与继承痕迹：

## 事实、推演与虚构边界

真实依据：

传统表达及适用范围：

合理推演：

文学虚构：

来源或待核验项：

## 负面约束

对象误认：

结构与功能禁区：

无依据新增：

风格与特效禁区：

## 待作者确认

只列会改变故事语义、对象身份或主视觉方向的分叉。

## 生成交接规格

仅在状态为“已确认设计”后填写。

成图模式：单图 / 人物审美基准 / 人物身份锚图 / 人物服饰状态 / 人物场景表现 / 器物四视图 / 器物四视图加展示图

视图计划：人物 3/4 全身 / 正面 / 侧面 / 局部 / 场景态；器物正面 / 侧面 / 3/4 身份锚图 / 结构细节 / 按需背面 / 按需状态

输出格式：PNG / WebP / 明确需要可编辑线稿时 SVG

写入位置：

画面任务与主体关系：

源图与编辑区域（局部编辑时）：

必须可见：

本轮必须保留：

本轮只改变：

禁止顺带改变：

允许探索：

构图 / 视角 / 景别 / 动作：

环境与光线：

负面约束：

可观察验收条件：

## 验收结果

多图时为每张图复制本节，不共享制作状态、采用范围或修订预算。

图片 ID 或路径：

图片角色：审美基准图 / 身份锚图 / 服饰状态图 / 场景表现图 / 器物视图

制作状态：已生成待验收 / 已验收图像

采用状态与范围：属性候选 / 作者采用 / 正典写回

定向修订预算：0/1 未用 / 1/1 已用

结论：通过 / 需修订 / 无法验证

契约符合项：

承重偏差：

可保留项：

下一次生成的最小修正：

设计缺口 / 生成偏差 / 工具限制 / 故事冲突：

下一制作状态：需修订或无法验证时保持已生成待验收；通过时进入已验收图像
